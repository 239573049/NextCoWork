# 更新日志


## v2.2.5

### 新增

**启动失败不再是一片白屏**

- 主进程在 `registerIpc()` 之前抛错时,窗口已经建出来了,而闸门永远不会放行 —— 此前用户看到的是一整窗空白,界面上一个错都不报,唯一的线索是终端里那行 `[app] 启动流程失败:`。现在同一条异常经 `announceStartupFailure` 推给首屏:一屏说明、一个「打开数据目录」的入口,以及可复制的原始诊断(含堆栈)
- 等待完整 IPC 超过 15 秒同样切到诊断页,而不是继续伪装成「正在加载」;等待期间画的是首屏骨架,不再是留白。诊断页只在 `ipcReady` 还没盖上时出现 —— 已经能用的 App 不会因为一个后台服务的失败被反过来卸载掉
- Windows/Linux 没有原生标题栏,启动骨架与诊断页也必须能拖动、最小化、关闭:窗口控制随迁移频道一起提前登记,并在完整 `registerIpc()` 里跳过,避免同一条频道被监听两次(一次点击执行两遍)

**邀请海报:一键存成 PNG 发出去**

- 奖励中心新增「保存邀请海报」,按**你自己的邀请码**生成一张 1920×1080 的海报(二维码、邀请码、三条卖点),点一下走系统保存对话框落盘
- 二维码里编的是邀请链接本身(`…/login?ref=<码>`)而不是邀请码文本:编错内容的症状是朋友确实注册了、双方却谁都拿不到奖励,而且全程零报错
- 海报文案跟着界面语言走(切英文再存一张就是英文海报),版式判断留在纯函数里、由单测钉住;新增 `app:saveImageFile` 频道,和文本保存分开 —— 合并成一条会逼每个调用点带一个「这次是文本还是二进制」的参数,传错一次就写出一个内容是 base64 的 `.png`,打开之前看不出来

**更新说明按 Markdown 渲染**

- 「关于 › 更新说明」此前把 `CHANGELOG` 截下来的那段原文当纯文本铺开,界面上直接出现 `### 改动` 和 `**加粗**`。现在按 Markdown 渲染,而 markdown 那一坨(streamdown + katex + mermaid,单独一个 chunk 约 1.0MB)是**点开弹窗时才拉**的:设置浮层是静态依赖,从这里静态引它等于把它拽回主 bundle
- 拉取期间先按纯文本铺开、到了才换成渲染版:慢盘上先看到的是能读的文字,而不是一个「更新说明是空的」的空弹窗

**应用内奖励中心,邀请好友不再跳浏览器**

- 账户菜单里的「邀请好友」改成应用内的全屏浮层:横幅、邀请链接与邀请码(可复制、可在浏览器打开)、累计统计,以及邀请记录 / 奖励记录两张表;右上角关闭、Esc 关闭、标题旁刷新
- 邀请链接在主进程拼成绝对地址,渲染层不再持有站点常量;取数失败分成「未登录 / 服务端还没开放(404)/ 网络错误」三种,平台没部署该接口时给的是「还没开放」而不是「网络错误」
- 横幅素材深浅各一张,跟随主题切换;奖励任务一栏暂为固定空态(拆除条件写在该组件的文件头)

**模型目录**

- 收录 Grok 4.7(500K 上下文;定价按产品方确认与 4.6 一致,4.7 目前还没有独立的 LiteLLM/OpenRouter 条目)与小米 MiMo V2.6 系列(Pro / Flash / Pro UltraSpeed)
- 插件菜单图标补上 `tv` / `film` / `music` / `gamepad`:网页应用类插件此前只能挂一个拼图块

### 改动

- **「默认模型 / 默认子代理」从「模型」页搬到「通用 → Agent」**:它们和 AI 审核模型、目标判定模型同属「Agent 用哪个模型」这一类问题,原先挤在供应商列表那条 236px 窄列的 footer 里,和「配权限/审核模型」不是同一次心智动作。同时把两级弹层选择器抽成 `ProviderModelMenu`,输入框与设置页共用一套「先选供应商、再选这一家的模型」的交互(子菜单定位、视口翻转、跟随滚动重新量高那套逻辑抄第二份的代价是「面板飞到屏幕外」,且不报错)
- **工作区(过程段)默认收起**:此前「本轮有过失败工具调用」会默认展开,而后台任务(如更新检查抓取失败)会让这个条件**永久**成立 —— 表现为每次打开会话「用时」面板都是展开的。用户要看失败细节点开一次就行
- 账户菜单与侧边栏里「积分余额」的文案统一成「余额」

### 修复

**Windows 上密码认证永远失败——密码是对的**

- OpenSSH 只肯把 `SSH_ASKPASS` 指向一个 exe(`CreateProcessW` 起不了 `.cmd` 与 `#!` 脚本),而打包形态的入口就是 Electron 本体:它启动时先往 stdout 写一个换行,而 ssh 只取 stdout 的第一行(`readpass.c`),于是用户看到的是「密码明明是对的却一直认证失败」,没有任何一条错误提到 askpass。现在 helper 是**同一个 exe 的 node 形态**(`ELECTRON_RUN_AS_NODE=1` + 一份生成的脚本),因此打包配置里 `runAsNode` fuse 必须是 `true` —— 关掉它就会退回带 Chromium 的 GUI 形态,上面那条症状原样复现
- 同一个原因还有第二个坑:GUI 形态会去抢主窗口的 Chromium profile,第二个实例卡在 profile 上,表现为连接一直转圈到超时;helper 因此还带了自己的 `--user-data-dir`
- `ELECTRON_RUN_AS_NODE` 是本仓库「拒绝透传 Node/Electron 注入开关」这条规则上唯一的例外,而且只对调用方显式传入的 overrides 放开(继承环境里来的那份照旧丢掉),`NODE_OPTIONS` 依旧全封
- 断开连接时清理临时目录失败不再让整条断开链路失败:Windows 上 GUI 兜底形态留下的 profile 会握着句柄,`force` 也删不掉,而从这里抛出去的错会变成「断不开连接」

**任务清单:模型丢掉一项,界面上看不出来**

- `TodoWrite` 每轮发的是**完整清单**,所以一张接一张的卡片看上去几乎一样。现在回执里带上「上次 → 这次」的增量,并把**被丢掉的项逐条点名**回显给模型 —— 前后两份清单各自都自洽,「任务凭空消失」此前只能靠用户自己发现
- 消息里的清单卡片改成「先报这次更新改了什么,完整清单收在折叠里」:新增/移除/刚完成/刚开始各报一次数,没有变化的那一档不报(四条都写「0 项」等于没说)。清单本体仍与输入框上方那条共用同一份行渲染,两份不会分叉
- 上一份清单从**本 run 的转录**里取(不新存一份状态,`todo.ts` 那条「完全无状态」的设计不动),并且按 `callId` 查「这次调用**之前**那份」而不是全局最新那份;工具名从注册表查(撞名时外部名带哈希后缀,写字面量会**静默**取不到任何东西)。`TodoItem` / `MARK` / `latestTodosFrom` / 新增的 `todoPlan` 一并下沉到 `shared/agent/todo.ts` —— 渲染层要用它们,而渲染层 import `src/main/**` 是禁止的

### 文档

- 新增 `README.md`(英文)与 `README.zh-CN.md`:功能面、下载、开发与验证命令、目录结构、插件开发入口
- 补上 `LICENSE`(Apache-2.0),`package.json` 同步声明 `license` —— 仓库此前对外可见却没有许可证文件

### 测试

- 新增:任务清单增量(`todoPlan` 的新增/移除/完成/开始、顺序变化不算变化、重复内容一对一消耗、不知道上一份时四个字段全空)、按 `callId` 回溯历史清单(被拒绝的那次与还没有回执的那次都不更新「上一份」)、消息里那张卡片(收起态、丢项单独列出、只标本次变更的行、没有上下文时说「不知道」而不是「无变化」)、清单默认折叠、更新说明的 Markdown 渲染与「关着时不拉 chunk」
- 新增:Windows askpass helper 脚本按 ssh 的调法真跑一遍(断言 stdout **整串相等** —— 多写一个字节就是「密码不对」),以及取消时以非 0 退出而不是送一个空密码
- 新增:邀请海报(二维码编的是邀请链接本身、模块不越界、文案全部来自外部传入、XML 转义、超出三条就丢掉而不是溢出、文件名里保留邀请码)
- 完整套件 5790 项全部通过(23 skipped);`npm run typecheck` 与 `npm run lint` 通过(0 error,4 个既有 any warning)


## v2.2.4

### 新增

**模型可以直接在对话里画出「会动的图」—— 内置可视化 widget**

- 新增工具对 `visualize_read_me` / `visualize_show_widget`:模型讲「复利是怎么滚起来的」「这个 transformer 的结构」时,不再只能一行行写文字 —— HTML/SVG 作为工具参数传进来,在对话流里内联渲染成一个真的在跑的页面(滑块能动、图表能画、脚本会执行),不是截图,也不是代码块
- `read_me` 按模块下发 7 万字设计规范(图表、图类、艺术风格、UI 组件、配色、SVG 预置),`show_widget` 收代码,它的说明里明确要求先读规范;没有照抄原版那个「先读过文档」的布尔门闩 —— 门闩的代价是模型被自己误写的一个参数卡住整整一轮,收益只是少漏读一次
- 沙箱是独立的 `ncw-widget://` 协议加一个 `sandbox="allow-scripts"` 的不透明源 iframe:widget 是模型生成的任意 HTML,父文档拿不到它的 DOM,它也拿不到聊天界面。不复用插件协议(内置工具没有 pluginId,两种 CSP 策略也不同),也不用 `srcDoc`(它继承父文档的 CSP,开发时能跑、打包后内联脚本全被拦,症状是一句谁也读不懂的 Refused to execute inline script)
- widget 跟随应用主题:把本仓库的 token 翻译成规范认得的那套 CSS 变量名,换一套外观,图里的卡片、描边、强调色跟着变,模型不需要知道这件事存在;规范里的 info 色族对到 accent,于是 widget 里的链接与界面其余部分同色,并跟着换色器走
- 内容按固定间隔节流推进 iframe(间隔内多次变更只保留最新一份;不用 debounce —— token 连续不停时它会让整整一代内容永远等不到发送),收尾那一帧立刻发,它决定图表何时画出来;高度由 iframe 自量自报,宿主钳制后生效
- 刻意不给 `sendPrompt()`(原版 widget 里「像用户打字一样往对话里发一条消息」的函数):它是 widget 通往 agent 主循环的一条输入通道,而 widget 内容是模型生成的、还可能是它从某个网页里读来的;`read_me` 的说明里明说本宿主没有它,免得模型照规范画出一批点了没反应的按钮
- 工具申报 `needsNetwork: true`,尽管它自己一个字节都不出网 —— 出网的是它产出的那张卡片(规范推荐从 CDN 拉 Chart.js);申报 false 等于用户关掉「联网」后仍有一条从这里出网的路。代价说清:开关关掉时连纯 SVG 图也做不了,这是刻意的取舍。`readOnly: true`,三档权限直接放行(联网那道闸仍管得住它)

**等用户表态的题面,参数还在流就能读**

- `AskUserQuestion` / `ProposeGoal` / `ExitPlanMode` 的入参就是给人读的题面。此前从第一个 token 到待决面板出现往往隔着好几秒,那几秒里工具卡上只有一行工具名,用户不知道自己马上要被问什么;现在题面边写边渲染
- 预览里的一切不可作答:可作答的题面只有主进程待决表一个来源,预览是从半截 JSON 投影出来的,连 `interaction.id` 都不存在 —— 拿它渲染的控件全部 disabled;切题可以点(只是翻页,点不出任何会失败的承诺),作答不能点
- 只读预览与可作答的卡是同一套组件:题面从预览换成可作答的那一瞬间不跳版式,而那正是用户盯着看的时刻;折叠摘要报「共 N 题」—— 多道题的卡片折叠起来只有一行,不写清一共几道,用户看到待决卡只显示第一题会以为就问了这一件事
- 工具状态词新增两档:等表态(「球在你那…」「等你拍板…」—— 这一档里 Agent 其实不在干活,词得说清是在等人)与画图(「勾勒中…」「配色中…」)

**任务清单:展开卡同一画法,收尾时如实补报**

- `TodoWrite` 工具卡展开后的清单与输入框上方那张任务清单是同一件东西 —— 同一份数据两套画法,改一处必漏一处,而两者在同一屏上下相邻;入参可能是半截的(流式中每一帧都可能多出半个条目),只做收窄、绝不猜内容
- 清单多了「和本轮的关系」三档:`running`(本轮真的写成功过,显示进行中与转圈)/ `stopped`(本轮已收尾,未完项按未完成陈列并报出剩余条数)/ `snapshot`(转录里的历史快照,不转圈、不说「正在做」)。此前转录里一个 `in_progress` 既可能正被推进、也可能是上一轮的死账,组件推不出来就照画,表现为清单永远「在跑」,用户会去等一个不会来的结果
- 收尾补报:主代理正常收尾时,若本 run 成功写过的清单仍有未完成项,给模型一次内部补报机会 —— 它挡的是「正文里宣布完成、用户正看着的清单却没更新」:回执与界面长期对不上,全程零报错。每 run 只提醒一次(不被工具调用重置);不自动替模型打勾、不反复续跑逼它清空阻塞项;Goal/Stop 的续跑与强停裁决优先,计划模式与后台任务不催

**「谁还在跑」改为全局广播,运行中角标终于会停**

- 新增 `agent:activeRuns` 广播:run 起止与首屏握手都带上主进程的权威集合,外层工作区 Tab、内层对话 Tab、侧边栏会话行三处运行中指示全读它
- 此前那颗圆点只能靠按 run 订阅的事件流收尾,而有两类 run 的结束永远送不到:定时任务起的 run(没有任何窗口订阅过,主进程整批丢弃)和 ⌘R 重载后还没打开的会话 —— 症状是 Agent 早就跑完了、消息也停了,角标一直转到应用重启,状态行却写着「已完成」
- 侧边栏会话行不再兼信 `sessions:list` 快照里的 `running` 字段:run 结束时主进程不发会话变更,列表不重拉,那个 true 会一直挂着 —— 表现为对话跑完了侧边栏还在转圈,直到用户恰好改名/归档某条会话才消;一个事实只有一处真源
- 只播顶层 run:子代理的起止不改变这个集合,一次编排十几个子代理不会变成十几次内容相同的广播

**文件引用点得开了**

- 气泡里的 `file_ref`(拖进会话的文件)与行内 `@` 引用此前只读,点上去毫无反应 —— 而它们长得像按钮,那是一次注定失败的承诺;现在点了就在右侧工作台打开,由已装插件决定用谁打开
- 打开前先确认它还打得开:引用是发送那一刻的快照,文件后来被删掉、改名是常态,直接开只会留一个只显示错误的 Tab 要用户自己去关。判定是真读一次而不是 stat(stat 只回答「存在」,会让「预检说能开」和「打开是空的」分叉);打不开一条 toast 说清原因,同一条引用连点多次是一条通知,不是铺满屏幕
- Markdown 里的文件链接走同一条判定,失败原因画在链接旁边;「打不开」的判定与文案全应用只有一份

**侧边栏左下角有了账户菜单**

- 原先那张「账户信息 + 齿轮」合体卡片整个是一块可点区域,点哪儿都开设置,于是「看/管自己的账户」在界面上没有入口(设置 › 账户页里有,但没人知道要走那儿);现在拆成两块:左边点开账户菜单(头像/昵称/邮箱、充值、邀请、支持、登出),右边齿轮仍然直接开设置,并可直达指定设置页
- 余额只读登录状态里那一条数据链,菜单里不存第二份 —— 两处各存一份迟早复现「设置页和菜单里的数字对不上」;那颗刷新按钮只负责发起刷新,结果由广播回来
- 邀请入口是「去浏览器里拿」而不是在菜单里显示链接:邀请码那条接口要网页会话 cookie,桌面端令牌会被平台白名单判失败,客户端拼不出真链接 —— 不编一个码,也不把带码的链接写死

### 改动

- **macOS 发布包启用签名与公证**:此前两平台都故意不签名(当时没有证书),而不签名的包既过不了 Gatekeeper,也过不了更新器的 designated requirement 校验 —— 表现为更新包下载成功但装不上。过程中发现 `CSC_IDENTITY_AUTO_DISCOVERY: false` 时 `findIdentity()` 直接返回 null,签名和公证会一起静默跳过、连 `CSC_LINK` 都不读,于是打包前先点名缺哪个 secret(「签名成功、公证被静默跳过」是最贵的失败:CI 全绿,用户拿到的包照样报警);证书只在 macOS 那一行注入 —— 设成全局的话 Windows 会拿 mac 的 p12 去签 exe
- 脚手架与 CLI 包(`create-nextcowork-plugin` / `plugin-cli`)升至 0.2.0

### 修复

**启动偶发停在「首屏握手失败: No handler registered for 'app:getBootstrap'」——重载一次就正常**

- 闸门此前只按「库里没有要迁移的东西」放行。可窗口为了画迁移进度屏被提到了 `registerIpc()` **之前**(窗口必须早于闸门,闸门必须早于 `openDatabase()`,见 `main/index.ts`),于是**没有迁移的那一次启动**里,渲染层只要比主进程快一点,挂上的 App 第一个 invoke 就撞上还没登记的频道
- 放行判据拆成两半、缺一不可:「闸门里没事」加「主进程答得上来」(`MigrationState.ipcReady`)。后一半是启动序列上的一个里程碑,由 `main/index.ts` 在 `registerIpc()` 之后**立刻**盖章,并重推一份完整快照——闸门自己那几次推送全都发生在它还是 false 的时候,只翻标志而不重推的话,用户在失败页点「继续」会一直没反应
- 判据抽成纯函数 `views/migration-release.ts`:它错的唯一形态(渲染层比主进程快)起一次 Electron 也未必撞得上,留在组件里就只能靠手测(见下面的测试)
- 顺带把这条错路上的诊断写清楚:启动流程在 `registerIpc()` 之前崩掉的话,窗口已经建出来了,它会一直停在空白首屏、界面一个错都不报(闸门不放行,它也递不出错误),此时日志里的 `[app] 启动流程失败:` 是唯一线索。原先这种情况下渲染层报的是「频道没登记」,而真正的病因是主进程启动崩了,两者几乎没关系

**合并旧数据卡在 `UNIQUE constraint failed: usage_records.id`,点重试每次都停在同一处**

- 「这条会话缺 ⇒ 它的子行也缺」这个假设在子表上不成立:`usage_records` 只增不删、也没有指向 `sessions` 的外键,会话被用户删掉时它的用量行留在库里;于是「目标库里缺这条会话」与「它的用量行已经在目标库里」可以同时为真,而补会话那条 INSERT 只按父表判过缺
- 子表改成判**它自己的主键**(从 `PRAGMA table_info` 读,不写死 `id`——`file_snapshots` 的主键是复合的,写死会让那一张的判重**静默**失效)。已经在那里的行一个字节都不动
- 没有改用 `OR IGNORE` 了事:判完唯一约束仍然是兜底,非主键的唯一性(索引、外键)坏了照样抛,让这次会话整体回滚、把错误摆在闸门上,而不是静默变成「少了几条消息」

**Windows 上每起一条后台命令就弹一个独立控制台窗口,停止命令还要闪黑框**

- spawn 的 `detached` 在 Windows 上被翻成 `DETACHED_PROCESS`,而这个标志会让系统忽略「不弹窗口」,于是每跑一个钩子、每起一条后台 Bash 都弹出一个控制台;现在 Windows 一律不传它
- 「杀整棵树」与「脱离会话」拆成两件事:Windows 那条路靠 `taskkill /T`,本来就不依赖进程组;此前混用一个开关,Windows 上会退化成只杀那一个 shell —— `npm run dev` 的 node 活下来继续占着端口,而界面报告已停
- `taskkill` 补上「不弹窗口」:主进程是 GUI 程序,每次停止/超时都会闪一个黑框,Win11 上还可能被「默认终端应用」接管成一个真的终端窗口

**Bash 输出里混着看不见的 ANSI 转义序列 —— 看着像乱码,行过滤一条都匹配不上**

- 管道不是 TTY,但很多 CLI 照样着色(vitest 用的 picocolors 在 Windows 上无条件开色);ESC 本身不可见,用户看到的是「`[32m✓ [39m src/…`」这种读不下去的东西,只会以为命令输出坏了
- 剥在入口而不是回读时:`BashOutput` 的按行过滤会被行首的 `ESC[31m` 弄得一条都匹配不上(而输出看起来完全正常),环形缓冲与回给模型的输出预算也不该被转义序列吃掉
- 终端(PTY)绝不过这道 —— xterm 靠这些序列画屏幕,剥掉等于把终端变成一段乱糟糟的纯文本

### 文档

- `plugin-builder` Skill 新增 `references/contribution-points.md`:`contributes` 的每一个键「宿主到底拿它做什么」逐条列出,并把装得上却什么都不做的那些(`slashCommands`、`agents`、`modes`、`themes`,以及 `views.location` 的 `sidebar` / `panel`)明确标出来连带原因——没有可挂载的 Tab kind、扫描器不读它们、打包器不复制 `agents/` 与 `modes/` 目录。此前作者只能从「装上了、界面上没反应」反推
- `engines.nextcowork` 的说明补上「写你真正需要的那个**最低**版本,而不是最新的那个」;打包排查表新增两行:声明了 skill 而模型看不见、声明了 agents/modes/themes/slashCommands 而毫无反应

### 测试

- 新增 `views/__tests__/migration-release.test.ts`:把五种启动时序直接摆出来——主进程还没应答时 `idle` 也不放行、能应答之后放行、迁移进行中与失败页照常画、在主进程能应答之前点「跳过并继续」不生效
- `legacy-merge` 新增一条按子表主键判重的用例(用量行已经在、它那条会话缺着时补会话而不是撞唯一约束,且第二次合并仍然不抛);原来「制造第二条会话必然失败」的手段换成一条**悬空外键**——旧手段(消息 id 已经在目标库里)现在被判成正常情况并跳过,不再是失败
- 新增:widget 协议(只服务两个固定路径、其余一律 404、CSP 头)、外壳运行时的构建产物自包含、iframe 同步算法对半截 HTML 的合入、内容帧节流用假时钟钉住「间隔内只发最后一份」「收尾帧不等窗口」、`visualize` 工具与规范按模块下发、widget 主题映射表覆盖规范正文实际用到的变量、交互与 todo 预览对半截 JSON 的收窄(含「拿不准就不画」)、待决卡的只读预览不与 QA 探针属性相撞、文件引用与 Markdown 链接的「先预检再开」、活跃 run 广播让丢过事件的窗口角标收敛、todo 补报的「每 run 一次额度」
- 完整套件 5733 项全部通过(23 skipped);`npm run typecheck` 与 `npm run lint` 通过(0 error,4 个既有 any warning)


## v2.2.3

### 新增

**插件可以只包一个网站,一行代码都不用写**

- 清单新增 `kind: "webapp"`:不写 `main`,只声明 `contributes.webApps`(标题、图标、https 地址、开在内层 Tab 还是右侧面板、是否进侧边栏)。写了 `main` 是**错误并整份拒绝**,不是忽略——忽略会让作者以为自己的代码在跑;声明 `commands`/`tools`/`customEditors` 这类需要代码的贡献点同样整份拒绝
- 装上即生效:侧边栏多一个入口(排在内置功能项之后),点开在当前工作区的浏览器分区里加载,与你自己的浏览器 Tab 共享登录状态——站点已登录就直接可用。插件零权限、不跑任何代码、不占常驻内存
- 站外链接不劫持:只有命中清单里 `hostPermissions` 的地址在应用内导航,其余交给系统浏览器,并在视图顶部留一条「已用系统浏览器打开」的提示——它不会变成一个不受限的浏览器
- 随包示例 `examples/ncw.bilibili`(整个包就是一份清单加两份文案)。声明成外层功能 Tab 的 `open: "feature"` 这一档还没做:降级成内层 Tab 并明确告诉你降级了,不静默

**engines 改按插件 API 版本判定 —— 此前用官方脚手架生成的插件装上就是「装载失败」**

- 脚手架默认写 `engines.nextcowork: "^0.2.0"`,而宿主拿**应用版本**去判(2.2.2);`^0.2.0` 要求宿主大版本为 0,于是按官方文档写出来的插件装上直接是 error,诊断说「requires ^0.2.0, this host is 2.2.2」。现在判定改用插件 API 版本(这一版 0.3.1)
- 已经装了的那批不需要做任何事:判为「弃用」,**照常装载运行**,只在详情页推一条 warn,提示把 engines 改成 `^0.3.1`。这条兼容表带着拆除条件(市场上声明 0.2 的包降到 0 时清掉)
- 脚手架默认值同步改 `^0.3.0`;读不懂的 version range 仍然判不兼容——宁可装不上,也不要装上一个会静默出错的插件

**一批「d.ts 里写了、真调必失败」的 API 接通了**

- `env.openExternal`(只放行 https)、剪贴板读写、`window.showQuickPick`、`scm.status`、`configuration.get`、`customEditors.setDirty`、`agent.registerToolInterceptor` / `registerContextProvider`:此前要么落到 `internal_error: method ... has no handler`,要么插件侧看到的是 `undefined`(垫片根本没导出这个命名空间,`customEditors.setDirty` 就是这样——脏标记永远为空,关 Tab 不拦)
- 垫片里 `interceptor.willInvoke` / `didInvoke` / `context.provide` 三条 invocation 此前会抛 `unsupported invocation`(主进程早就在发);现在没注册拦截器的插件返回空裁决——fail-open,一个没实现拦截器的插件不该把所有工具调用堵死

**Git 能力全套**

- `scm.status` / `diff` / `log` / `branches` 与 `stage` / `commit` / `createBranch` / `checkout`,复用应用自己的 git 实现;写操作走既有审批链
- 不开 push / pull:它们会把本机凭据用到远端,而冲突、鉴权、hook 这些失败形态不是一条 RPC 能如实回答的
- 不在 git 仓库里、没装 git、远程工作区一律给明确诊断,而不是回一个空状态(空状态会被当成「没有改动」)

**工作区变更订阅(是事件,不是 watcher)**

- `workspace.onDidChangeFiles(handler, globs?)` 只覆盖**经应用发生的写入**:Agent 工具写盘、插件自己写盘。外部编辑器、`git checkout`、终端里的 `mv` 都不会触发,这条边界同时写进 d.ts 与插件文档,免得被当 watcher 用
- 插件休眠时不叫醒它,也不逐条问权限;按前缀匹配过滤,单插件最多 32 条订阅

**插件能问用户了**

- `window.showInputBox` / `showConfirm` / `withProgress`,以及一起接通的 `showQuickPick`:主进程只发请求并等回执,**界面用应用自己的对话框画**,插件决定不了它长什么样。用户关掉、或 5 分钟没理会 → 返回空值(`null` / `false`)而不是报错——没理会一个弹窗不该让插件收到异常
- 单插件同时在屏上只有一个输入框、一个确认框与最多 3 条进度;插件被禁用、应用退出时挂起的请求一律按取消收口,不留悬空 Promise
- 进度这一档只做了一半:配额、插件被禁用时自动收尾、请求超时都由主进程管住了,但**界面上还没有地方画进度条**——插件调 `withProgress` 会成功,用户暂时看不到进度

**流式命令执行**

- `process.execStream` 边跑边推输出(50ms 合批),`execAbort` 立刻中断;审批**只在开始时问一次**,不逐块问
- 单插件最多 8 条并发;输出上限 512KB,超了仍然继续收但只推一条 `truncated` 标记(停止收会让子进程卡在 write);中断与超时统一 `code = 124`

**结果卡多了四种原语,危险按钮先确认**

- 新增 `markdown`(走应用自己的受信渲染器,链接交给系统浏览器,不执行裸 HTML)、`list`、`metric`、`divider`;`button` 新增 `confirm`,插件声明的危险动作点下去先弹应用自己的确认框,主按钮是危险样式
- 预算不变(16KB / 32 块):这些原语是为了**少一点 JSON 转储**,不是为了塞更多数据;认不出的块类型仍然只丢那一块

**插件视图可以直接用宿主的 React 与控件**

- `react` / `react-dom` / `nextcowork/ui` / `nextcowork/view` 由宿主通过 import map 注入插件视图:插件不再自带 React(两份实例的症状是 `Invalid hook call`),视图体积从几百 KB 降到几 KB
- `nextcowork/ui` 就是应用自己的 Button / Dialog / Input 等控件,跟随主题、外观与动效档位;样式表与主题变量由宿主注入,插件 HTML 不需要自己写 CSP 或引样式
- 打包必须带上这份运行时,漏了的话插件视图一律 `Failed to resolve module specifier "react"`——开发时正常,只有装完才暴露

**自定义编辑器的三个静默失败点**

- `onCustomEditor:` 激活事件此前**没有任何派发点**,编辑器类插件的 iframe 第一次请求必 403;现在挂 iframe 之前先唤醒插件
- `ncw:doc:dirty` 此前被静默丢弃,表现为关 Tab 从不拦未保存内容;现在关 Tab 前先让插件存一次,**存不下才拦**,并说明是哪一条失败(不是每次都弹「要保存吗」)
- 图片文档:打开时给 `dataUrl` 与 mime(此前收到空串,编辑器一片空白),保存支持 base64 回写

**插件的 Skill 真正可用**

- 包内 `skills/<name>` 进技能目录:装载期校验路径形状与 `SKILL.md` 是否存在(装得上却扫不出来,才是要挡的那种失败);优先级低于项目与全局,两个插件撞名先来的赢且双方都有诊断
- 插件被禁用/卸载 → 贡献的 Skill 立刻消失(每次扫描现问,不缓存);技能面板标注「来自插件」且**不画卸载按钮**(那个按钮会去删一个不存在的路径),Skill 工具会把包目录的绝对路径告诉模型

### 改动

- **内置工具卡片的标题与摘要迁进 i18n**:这些句子此前硬编码在 shared 里,表现为英文界面上工具卡片全是中文;顺带补上参数兜底——半截 JSON 时退化成「读取…」,不再把 `{param}` 字样漏到界面上
- **空会话首屏的问候语同样按语言出**(此前四种问候语写死中文,英文界面也是中文)
- **输出额度改由唯一的全局设置项决定**(新增 `maxOutputTokens`,1024–200000):模型目录声明的输出上限不再压住你的设置值,只被上下文窗口收窄
- `workspace:writeFile` 支持 base64 图片字节写回(严格 base64、只覆写已分类为图片的文件,乐观锁与临时文件原子替换不变)
- 插件脚手架新增 `--view` 模板,生成的视图不自己引样式、不自己写 CSP,三样都由宿主注入
- `plugin-builder` Skill 从一份长文档拆成索引页加 6 份 references(清单与权限、权限映射、自定义编辑器、agent 工具、runtime API、打包)

### 修复

- **Linux 托盘点了没反应**:多数 Linux 桌面环境的托盘不派发 `click` / `right-click`,只认 `setContextMenu`;不设的后果是整颗图标点了没有任何反应,连菜单里的「退出」也点不到,而这个坏掉的状态在 macOS / Windows 上完全复现不出来。现在 Linux 走 `setContextMenu`,macOS 保持左键唤起窗口

### 测试

- 新增:插件 API 版本判定(老 range 判弃用、读不懂判不兼容、`>=0.2.0` 不享受特殊待遇)、webapp 清单规则(写了 main 整份拒、要代码的贡献点整份拒、URL 只放行 https)、**webapp 插件装载后宿主窗口一次都没被 spawn**、`env.openExternal` 真的落到宿主而不是 `internal_error`、scm 写类没批准被拒、`tabs.openBrowser` 必须命中 `hostPermissions`、插件 Skill 的加载与禁用撤下(含软链出包读不到)、视图运行时的服务与「包内同名文件盖不住宿主运行时」、图片字节写回(解码后相同则整跳写入)、三个示例插件的端到端安装;完整套件 5592 项全部通过


## v2.2.2

### 新增

**免 Key 的内置搜索兜底：一个搜索服务都没配也能真的搜**

- 搜索服务未配置、全部失败或全部零结果时，`web_search` 不再直接回一句「去设置里配 Key」，而是先走公共 SearxNG 实例（最多试 3 个、每个 4 秒），全挂再按 Bing → DuckDuckGo → 百度 直抓结果页（每家 5 秒）；命中后给前 2 条抓正文补摘要（总预算 6 秒，正文抓取失败只保留原摘要、绝不影响结果本身）
- 结果标注实际来源（实例域名 / 引擎名），并附上给模型的提示：免费源质量与时效低于配置的专业服务；设置 › 连接 › 搜索 底部新增小节，可填自建 SearxNG 实例（支持 `http://localhost:8080` 这类本机地址，自建优先于内置实例）并一键测试
- 零结果与失败分开归因：「搜索通了但没结果」会明说换查询词值得一试，不再和「全部失败」混成一句——旧行为下模型会把同一个查询原样重试三遍；付费源的失败原因即使被兜底救回也照常带回，否则过期的 Key 会永远坏下去而表面上「搜索还能用」

**Bash 后台命令与「停止这条命令」**

- `Bash` 新增 `run_in_background`，配套 `BashOutput`（增量读输出、读过即清、按行过滤、回包带退出码）与 `KillShell`（杀整棵进程树）；最多 8 条并行、24 条留痕、单条输出 256KB 环形缓冲，丢过的输出会明确告知——起 `npm run dev` 这类不退出的命令不再白等到超时，SSH 工作区同样可用
- 运行中的 Bash 工具卡新增「停止这条命令」：只掐这一条，回合继续，模型收到「用户停止了这条命令、不要再跑」；此前唯一选择是停止整轮，模型写到一半的回复一起没。退出应用时统一收掉后台进程，不留占着端口却在界面上找不到的进程

**工作区编辑与外层 Tab 条**

- 外层 Tab「+」菜单的工作区行新增铅笔入口：改名、默认模型与思考强度、删除工作区；路径只读展示——改路径需要重新选择目录新建，画一个输入框会是一句点了没反应的假承诺
- 「+」按钮移出可滚动的 Tab 列表（此前 Tab 挤满后，最后一个 Tab 的关闭按钮会盖住它）；被滚出视野的 Tab 收进右侧下拉菜单直达

**迁入数据的账户归属交接**（v2.2.1 启动迁移闸门的后续）

- 迁入的会话在登录账户后自动认领到账户名下、出现在侧边栏——此前迁移显示成功、账户侧却一条会话都看不见（行级合并发生在打开账户库之前，归属只能先落 local）；一份迁入数据只属于第一个认领它的账户，同根目录复用既有工作区，远端来源降级为未绑定
- 归属清单与每条会话同一个数据库事务落盘：中途磁盘满等失败不会再把前半批永远留在 local；迁移重试与撤销后侧边栏当次刷新，不必重启

### 改动

- **上下文窗口校验改按本次请求实际预留的输出额度判断**：模型目录声明 32K/64K 输出上限的模型，哪怕这一轮只预留 2K，也会在发请求前就被拦下报「超出上下文窗口」
- **状态行压力条的分母实时跟随当前窗口**：中途打开「最大上下文」后，占用读数与「接近上限」提示当帧回落——此前要等下一次发送，表现为界面在催用户做一件他刚做完的事
- 更新说明正文随版本送达客户端更新卡片——此前只能送一个 GitHub Release 链接，卡片永远显示「暂无更新说明」；发布前先校验各平台产物齐全
- Agent 的 shell 选择与自建 SearXNG 地址标记为机器本地设置：切换账户、云同步与导出不搬运（localhost 地址搬到另一台机器就是死地址）
- 审批选项改为紧凑的决策项列表，多问题切换不再硬切得像整张卡重新挂载；子代理任务中心默认收起已归档任务，进行中/失败/被停的任务保持直接可见
- `WebFetch` 与浏览器工具按工作区所有者的明确要求放开了本机与内网地址筛查（协议白名单、URL 内嵌凭证拦截与响应体积上限保留）；收录 MAI-Code 1.1 Flash 及其定价

### 修复

- **SSH 密码认证被本机配置悄悄禁用**：`ssh_config` 里 `Host *` 下的 `PasswordAuthentication no`（部分工具与公司策略会写入）会让 ssh 连试都不试密码——表现为同样的用户名密码别的终端连得上、这里永远「认证失败」，用户只会去反复核对密码；现在只要明确选了密码/键盘交互认证，就在命令行显式打开对应开关，选「自动」时仍尊重本机配置
- **迁移目标库探测失败不再被当成「没有东西要搬」**：这正是丢数据的形态（静默继续、日志里一个字都没有）；现在停在明确标注「目标库损坏」的错误页，与源库损坏区分开
- **改动审查打开大文件首屏直接落在改动处**，不再让用户从文件第 1 行开始翻；几千行只有少量分散改动的文件按预算裁掉相同头尾，极端整文件重写宁可提示打开文件，也不在渲染线程分配无界的 diff 矩阵
- 切换审查文件不再沿用旧滚动位置（会直接越过改动），旧 diff 也不会顶着新文件名闪一帧

### 测试

- 新增后台 shell 注册表与限额、Bash 后台/单条停止契约、内置搜索三层链路与 SERP 解析（含验证码与改版降级）、迁移归属交接闭环（认领/撤销/降级/重试）、SSH 认证参数、上下文压力分母、任务清单、审查视图 diff 预算等测试；完整套件 5463 项全部通过


## v2.2.1

### 新增

**启动数据迁移闸门：旧数据在进入应用之前合并干净**

- 升级后首次启动若检测到旧版本留下的数据（旧打包安装的数据根、旧扁平目录布局，以及「根层还躺着一个库」的中间状态），先全屏跑完「整理目录结构 → 合并旧会话 → 复制附件」再进入应用；迁移期间数据库尚未打开，其余功能本来就不可用，因此不需要任何禁用逻辑
- 合并只搬**当前库里没有的行**——此前只要新库已存在，旧数据就被整体静默跳过且不留日志，表现为升级后某个时段的会话永久消失（本次收录的那类状态正是这样丢数据的）
- 失败各有说明：磁盘空间不足、权限不足、旧/新数据损坏、数据文件被占用；出错后可以**重试**、**跳过并继续**（旧数据原样保留，下次启动再试）、或**撤销本次合并**（只删这一次合并写入的会话，不影响之后新建的会话），诊断信息可一键复制

**SSH 连接跟随应用/系统代理**

- OpenSSH 自身没有任何代理概念，现在主进程按与模型请求**同一份**代理设置，把 ssh 改道到一条本地隧道上——手动代理、系统代理、PAC 脚本、直连白名单只有一份实现，设置页改了 SSH 立刻跟随
- 主机密钥仍按原主机名校验，不会每连一次就问一遍「确定要继续连接吗」，known_hosts 也不会被随机端口条目撑爆；用户自己配了 ProxyJump / ProxyCommand / HostKeyAlias 的一律不插手，局域网地址一律直连，代理凭据只送给用户手填的那台代理

- **改动审查卡直达文件**：回合底部审查卡上点某个文件，右侧 changes tab 打开并停在那个文件的 diff 上——此前永远停在清单第一个，点第 7 行和点第 1 行看起来一模一样

### 改动

- **退出流程改为两段式**：退出时若有未保存的文件改动，弹的是应用自己的「保存 / 丢弃」对话框，选完自动继续退出——此前系统的 beforeunload 拦下之后整次退出悄悄作废，应用还留在 Dock/托盘里，得再点一次退出
- **状态行运行文案按场景分组轮换**（仍 4 秒一换）：读文件和列目录、搜文件名和搜内容不再说同一句话；等首字节超过约 20 秒、同一个工具连跑第 5 次、上一个工具刚报错这些「相位上看不出来的时刻」有专门的文案；读屏仍朗读固定的「正在等待回复…」，轮换对无障碍不可见

### 修复

- **摘要压缩现在真的把已概括的历史移出上下文**：原先摘要是附加在完整历史上方的，占用不降反升（多出一整份摘要），于是每轮都再摘要一次——每轮多发一次模型请求、多一条检查点，永不收敛；模型还按「转录已经不在了」这个不成立的前提写交接。现在切点只落在整轮边界、绝不越过摘要覆盖的范围，老数据（无覆盖锚点）一律不裁；恢复旧检查点与当场压缩得到的是逐字相同的一份
- **压缩之后模型的 todo 进度表不再丢失**：todo 从完整转录推导，压缩动不了它——此前界面面板还在，模型却从这一轮起当无事发生
- **压缩摘要请求不再挤爆小窗口模型**：必留段原先不计预算，一条带大量并行工具结果的消息会让摘要请求自己先 400——而它偏偏发生在最需要压缩的那一刻；现在超预算按「中段降级 → 丢最早 → 首尾降级」三级让步。另外摘要压缩连续失败两次即停手，不再每轮白发一次请求
- **上下文圆环菜单标注读数来源**：圆环上的占用说的是**上一次请求实际发出去多大**，两次发送之间它一动不动——自动压缩之后菜单里仍挂着压缩前那个越线的读数，会被读成「压缩没生效」，而下一条发出去它就自己回落；现在明确标出「上一次请求实测」
- **Claude 等 anthropic 系模型经平台登录同步后协议不再错误**：同步时漏判 anthropic 系，别名不带协议覆盖，界面写着「跟随供应商 / 当前生效：OpenAI Responses」，思考档位与 prompt 缓存语义在兼容层里被悄悄丢掉，且登录全程零报错；老库已同步的别名在启动时一次性补上，用户自己选过协议的不碰
- **新对话里第一次贴图 / 拖拽 /「添加附件」不再石沉大海**：草稿铸出会话 id 的那一刻整棵组件树重挂，附件托盘连同上传承诺落在已被卸载的旧树上——表现为第一次没反应、第二次才正常；现在这批动作显式交接给重挂后的实例
- **回合改动审查卡即时出现**：「这条消息属于哪个 run」改在流式路径上记录——此前只能等重开应用（或切走会话再切回）从库里回填后卡片才出现，且全程零报错；逐轮用量、逐轮模型名同一条路
- **输入法组词按回车不再把候选词上屏两次**：组词中的回车曾被当成「发送」

### 测试

- 新增启动迁移（三类旧数据源、进度与失败恢复、撤销）、退出流程状态机、SSH 代理隧道（含真实 sshd 用例抓到的主机密钥别名问题）、客户端模型协议钉定（新同步要钉、老库要补、用户选择不顶掉）、changes tab 落点、messageRuns 流式记录、草稿附件交接、输入法组词等测试；完整套件 5364 项全部通过


## v2.2.0

### 说明

- 本版本无功能变更：代码与 v2.1.0 完全一致，仅按发布计划推进版本号。已升级到 v2.1.0 的用户可以跳过本版本——更新提示会照常出现，安装后功能与 v2.1.0 相同


## v2.1.0

### 新增

**模型提供商密钥：加密存储、显式查看、跟随账户跨设备同步**

- **密钥改为程序 AES-256-GCM 加密落库**：`credentials` 表存新的 NCK1 密文格式，主密钥是数据库目录下 0600 权限的密钥文件，不再依赖系统密钥环——Linux 无 keyring 的机器也能安全存密钥；旧 safeStorage 密文启动时自动迁移，双格式读长期保留（旧版本备份导入的行仍可解）
- **设置页可显式查看密钥**：API 密钥、OAuth access/refresh token、内置供应商登录 token 都能点击眼睛按钮查看并复制；明文只在点击后短暂存在于当前面板，切换供应商、保存或退出登录立即清除，常规状态刷新仍只显示后四位
- **配置同步重写为端到端加密（v2）**：设置 › 数据里用同步密码创建加密空间，密码在本机派生加密密钥，服务端只见密文；供应商、模型与密钥作为同一份加密快照同步到账户下的其他设备，另一台设备输入同一密码即可解锁并合并（本机同 id 配置优先）；支持冲突检测与「保留本地 / 使用云端」处理
- **旧数据三层迁移**：本机旧 safeStorage 密文自动升级；未登录时期的供应商/模型/密钥在首次登录账户时一次性归入该账户（原件保留在本地作用域）；云端仍存的 v1 明文配置在开启加密同步时自动「导出 → 加密归档 → 提交删除」，归档保留完整原文且每一步可安全重试
- **登录真正接入账户配置隔离**：登录/退出/重启按已保存登录态切换配置作用域，切换前等待在途同步收尾，防止 A 账户的网络响应把密钥写进 B 账户；备份包（v2 格式）携带并校验主密钥，恢复时数据库与密钥作为同一单元原子替换、失败一起回滚

**对话与导入**

- **对话右侧轮次导航栏**：一条刻度对应一整轮「提问 + 回复」，支持点击跳转、键盘操作与当前轮高亮；每轮新增「分支到新会话」——只克隆到该轮为止的转录（含图片附件重新落盘），之后的内容不带过去
- **新增 OpenCowork 导入源**：只读适配 `~/.open-cowork` 的会话转录（data.db）、供应商（ai-provider/*.json）、MCP 服务器、钩子、Agents/Commands 与技能目录，并入既有导入框架的扫描缓存、指纹映射与冲突检测

### 修复

- **SSH 连接失败不再是一句笼统的「无法连接服务器」**：按 OpenSSH stderr 细分主机密钥被拒 / 认证失败 / 网络失败三类原因，界面分别说明
- **OpenAI 兼容中转站的杂散内容不再中断整个回复**：部分中转站会在 finish_reason 之后继续吐出内容块（多路复用缓冲竞态），现在静默丢弃而不是让一次本已完整的回复整段报错
- **Skill ZIP 安装加固**：支持 SKILL.md 位于压缩包根目录的扁平包；此时安装目录名取自校验过的 frontmatter，而不是用户可随意改名的下载文件名

### 测试

- 新增凭证加密格式与主密钥生命周期（权限、符号链接拒绝、竞态创建）、加密快照捕获/合并/应用边界、双设备同步闭环（含错误密码、v1 云迁移归档）、旧 local 密钥归户、备份恢复主密钥一致性、轮次导航与 Skill 扁平包等测试；完整套件 5249 项全部通过


## v2.0.1

### 改动

- **数据目录再收拢**：Chromium 的会话集（`Cache`/`Cookies`/`Local Storage`/`Network`/`Partitions` 等三十来个条目）从 profile 根层再收进一层 `chromium/` 子目录，根层只留 `Preferences`/`Local State`/`Crashpad` 这几样 Chromium 认路径认得死的东西（尤其 `Local State` 存着 cookie 加密密钥，跟着搬会连累 `Cookies` 解不了密）；首次启动做一次性迁移，「删除全部数据」同步更新事实源，失败会整体回滚而不留半搬状态

### 修复

- **插件宿主窗口销毁竞态**：dispose 时如果窗口已经 destroyed，此前会尝试再读一次 `webContents.id` 而失败；现在建窗那一刻就记下来
- 插件休眠状态写入补上未捕获的 Promise rejection 兜底


## v2.0.0

首个正式版本。此前 0.1.x 系列共 25 个迭代版本都是通向这里的过程，这一条汇总 NextCoWork 作为「编码型 Agent 桌面端」目前具备的完整能力面，并收录 v0.1.25 之后两块尚未发布过的新能力（浏览器自动化、改动审查）。

### 新增

**这一版真正新增的两块能力**

- **浏览器自动化按 CDP 重写**：`<webview>` 通过 `webContents.debugger` 与页面建立绑定桥，`browser_snapshot` 从原来的 HTTP fetch + 正则洗文本，换成页面内注入脚本产出的真实 ARIA 快照（角色、文本、可交互态、`ref` 定位符）；新增 click / type / press / scroll / drag 一组输入派发工具，全部走 CDP `Input.dispatch*`，不再依赖 `sendInputEvent`；另外接入 `playwright-core` 无头后端，同一套快照与输入协议可以跑在没有窗口的 Chromium 上
- **每轮改动可审查**：Agent 一轮里新建/编辑/删除的文件会被 change-recorder 按 `runId` 聚合成改动集，回复气泡下方挂一张审查卡，右侧新增 changes tab 看逐文件 diff；支持按 `runId` 撤销或恢复整轮改动，撤销前会预检文件是否被后续改动覆盖，冲突时明确提示而不是静默覆盖

**Agent 内核与会话**

- 多种会话模式：代码 / 计划 / ACP 三种内置模式，也可用 Markdown 文件自定义模式（限定工具、必调工具、提示词），项目级模式覆盖同名全局模式
- 计划模式工作流：先调查澄清，再提交一份结构化计划（标题、步骤、依赖、验收标准、风险）等待审批，计划文件落在工作区 `.plan/` 目录，批准后转代码模式执行
- 会话目标 `/goal`：给会话设一个完成条件，模型持续工作到独立判定器确认达成，而不是答完就停
- 子代理会话、任务队列（排队/并发调度/取消/状态跟踪）与跨应用重启的后台恢复
- 结构化 `AskUserQuestion` 交互面板：单选/多选/多问题切换/自由文本，支持数字键、方向键、Enter/Esc 全键盘操作
- 插话（生成中追加的内容在下一个轮次边界注入）、对话回合操作条（复制/导出 Markdown/换模型重新生成/删除该轮）
- 全局搜索：全文搜索所有会话消息并高亮片段
- 智能上下文管理：协议窗口/有效窗口/压缩阈值三层拆分，支持机械压缩检查点、手动 `/compact`、「最大上下文」开关

**插件系统**

- 支持安装本地目录 / ZIP / 市场三种来源的第三方插件，运行在独立沙箱进程，能力按清单逐条声明、安装时审批、升级加权限再问一次
- 插件可贡献：Agent 工具、`+` 菜单项、命令面板条目、快捷键、状态栏、设置项、主题、自定义编辑器（接管某类文件用插件自己的界面打开）
- 插件市场安装进度条与安装窗口、插件详情页（能力声明/活动日志/诊断）、插件视图跟随应用主题
- 配套开发工具链：`npm create nextcowork-plugin` 脚手架、`@aidotnet/plugin-cli` 构建打包发布、`@aidotnet/plugin-api` 类型声明，随附 Excalidraw 示例插件

**Skill 与 Git**

- Skill 市场：搜索、分类筛选、详情、全局/工作区启用、触发统计、本地诊断；支持市场或本地 ZIP 安装并校验 SHA-256、目录结构与解压体积
- Git 面板：分支查看、领先/落后计数、逐文件暂存、拉取/推送、新建切换分支、diff 与最近 50 条历史、⌘↵ 直接提交；AI 一键生成提交信息（参考暂存 diff 与近期提交风格，只填输入框不自动提交）

**远程与账户**

- SSH 远程环境：创建管理远程工作区、SFTP 读写、askpass 交互认证、原生终端接入，MCP 服务随环境一并转发
- NextCoWork 账户体系：网页登录、离线使用、登录态续期、多 Team 账户选择；平台托管供应商，以及 ChatGPT/Codex、智谱 BigModel、Z.ai、Ollama（SSH 签名认证）等多家 OAuth 登录
- 配置同步（供应商/模型/偏好，加密快照，会话与本地路径不同步）、供应商配置导入导出、OpenCode 与 Claude Code 数据导入
- 用量中心：模型/活动/费用多维度图表，输入框下方实时显示累计 token 与费用

**体验与外观**

- 主题工作室与主题库：保存/复制/重命名/切换主题，实时预览壁纸、配色、面板材质、字体、动效，带对比度保护
- 标签页：置顶/取消置顶、横向滚动与下拉切换、双击改名（工作区同样支持），改名撞同名文件时会询问是否覆盖
- 标题栏更新指示器（有新版本才出现的图标）、应用内自动更新（下载进度、一键安装重启）
- 快捷键自定义、Windows/Linux 自绘标题栏与窗口控制、reduced-motion 偏好覆盖全部动效

### 改动

- 数据目录收拢：应用自身数据（数据库、附件、技能、设置）统一移入 `~/.next-cowork/data/`，根层留给 Chromium 缓存，首次启动自动迁移旧路径
- 内置模型目录按厂商拆分维护，模型导入改为纯函数推导 ID（`hash(转换器版本, sourceId, uuid)`）避免覆盖用户修改
- 发布流程强化：macOS/Windows 签名与 fuse 写入顺序修正、强制更新参数（强制标记/最低支持版本/宽限期）、安装包直传 Release 减少 CI 配额占用

### 修复

25 个 0.1.x 版本里积累的问题已在对应版本逐条记录（签名与安装崩溃、跨重启恢复、协议与导入一致性、插件权限误判等），此处不再重复；详见下方各版本历史。


## v0.1.25

### 新增

- **插件安装进度**：市场里那颗「安装」按钮会原地变成进度条，同时弹出一个安装窗口，能看到下载、校验、解压各自到了哪一步；下载时拿不到总大小就画斜纹，而不是假装成 0%
- **文件树里双击 `.excalidraw` 直接用插件画布打开**——此前弹出的是一屏原始 JSON。同一个文件，双击打开和插件自己打开，现在是同一个结果
- **标签页双击改名**，工作区也能改名；改名撞上同名文件时会问一句要不要覆盖，而不是默默覆盖或默默失败
- **插件视图跟随应用主题**，深色下画布不再固定在浅色；插件在市场与列表里显示自己的图标
- **标题栏更新指示器**：用一颗只在真有新版本时才出现的图标取代了原来横跨窗口的横幅——那条横幅盖住内容区顶行，而且点掉「稍后」就再也找不回来

### 改动

- **数据目录收拢**：应用自己的数据（数据库、附件、技能、设置）移进 `~/.next-cowork/data/`，根层留给 Chromium 自己的缓存与配置。此前两套命名风格扁平交错在一起，按字母排序后用户分不清哪些是自己的数据。首次启动会自动迁移，旧路径会一并改写
- 更新失败的提示改为按错误码给出对应文案；此前有些错误会在界面上原样显示成 `about.updates.error.invalid-metadata` 这样的内部标识

### 修复

这一版把插件从「装得上但用不了」修到能用，逐条都是独立的故障：

- **装插件报「需要登录」，而用户明明已登录**：客户端申请权限时漏了插件那两项，服务端因此判定整串权限不合规，而它返回的是「认证失败」，客户端只能把它说成未登录
- **带语言包的插件装不上**：检查包内文件是否存在时用了一份小写化的文件名，而语言包必须叫 `zh-CN.json` / `en-US.json`，于是永远匹配不上、报「缺少语言包」——而包里明明有
- **插件永远激活不了、点菜单没反应**：插件的隐藏宿主页面靠两段内联脚本启动，而它自己的安全策略只放行外部脚本，于是脚本一条都没执行，宿主也就永远等不到「我起来了」
- **插件宿主页面加载直接失败**：访问插件资源的协议只注册到了默认会话，而插件宿主用的是独立会话
- **插件第一次新建目录下的文件被拒**（例如 `drawings/drawing.excalidraw` 的 `drawings/` 还不存在时）
- **声明「随应用启动」的插件启动时不会被唤醒**
- **远程工作区里插件完全无法读写文件**：插件用的是本机文件系统，挂不上远程工作区的根目录；现在会明确拒绝，而不是把文件写进另一个工作区
- **插件菜单显示 `excalidraw.new` 这样的命令标识**，而不是「新建绘图」——包里的语言文件从未送达界面
- **插件命令执行失败时界面毫无提示**，点下去像没反应一样
- 删除 `+` 菜单里重复的「新建绘图」内置项（它与插件提供的那一项并存，而且它打开的标签是个空壳）


## v0.1.24

### 新增

- **插件系统**：支持安装第三方插件（本地目录 / ZIP / 市场）。插件跑在独立的沙箱进程里，碰不到宿主界面，也拿不到文件系统或网络的直接句柄——它要的每一项能力都写在清单里，安装时逐条批准，升级时如果要得更多会再问一次
- **插件能贡献的东西**：Agent 工具、`+` 菜单项、命令面板条目、快捷键、状态栏、设置项、主题，以及**自定义编辑器**——插件可以接管某类文件，在标签页里用自己的界面打开它
- **插件详情页**：看得到它声明了哪些能力、最近做过什么（活动日志）、装载时有没有报诊断；禁用或卸载会连带清掉它贡献的菜单、工具与文案
- **命令面板与快捷键收口**：此前命令面板只能搜会话，`+` 菜单和快捷键各是一套写法。现在三者共用同一份命令注册表，插件贡献的命令在三处同时出现
- **会话目标**：给一段会话设一个完成条件（`/goal <条件>`），模型不再答完就停，而是持续工作到一个独立判定器确认条件达成；`/goal clear` 清除
- **Excalidraw 示例插件**（`examples/acme.excalidraw`）：纯第三方插件，走通「新建文件 → 在标签页里打开画布 → 存回工作区」的完整链路
- **插件开发工具链**：`npm create nextcowork-plugin` 生成可直接上架的骨架，`@aidotnet/plugin-cli` 负责构建 / 打包 / 发布，`@aidotnet/plugin-api` 提供类型声明

### 改动

- `+` 菜单改为数据驱动，顺带修掉它此前不走翻译的问题：切到英文时这个菜单和侧边栏功能名不再是中文
- 删除会话的禁止条件收窄：原先任何一个 Agent 在跑就禁止删除**任何**会话，现在只拦「待删会话自己在跑」和「它的子代理在跑」两种
- 配置同步不再把配置明文写进本地待同步队列（供应商地址、工作区偏好等），改为只记一个「有变更」的标记，实际同步走加密快照

### 修复

- 修复带语言包的插件装不上 ZIP：路径检查用了小写化过的文件名，而语言包必须叫 `zh-CN.json` / `en-US.json`，于是永远匹配不上、报「缺少语言包」——而包里明明有
- 删除会话被运行中的 Agent 挡下时，界面现在会说明原因；此前点「确认删除」没有任何反应，也没有任何提示


## v0.1.23

### 新增

- **模式系统**：会话支持「代码 / 计划 / ACP」三种内置模式，也可在扩展页用一份 Markdown 文件自建模式（自定义允许的工具、必调工具与提示词，项目模式覆盖全局同名模式）；模式按会话记忆，输入框下方的模式药丸可一键切换、设为新会话默认或跳转管理
- **计划模式工作流**：模型先调查并用提问澄清，然后只能写一份计划文件并提交审核——计划期间写文件被围栏到该文件，审批支持「当前会话执行 / 新会话执行 / 要求修改 / 放弃」四个去向，批准后以代码模式执行
- **Git 面板**：侧边栏新增 Git 入口，支持查看分支与领先/落后计数、逐文件暂存与取消暂存、拉取/推送、新建与切换分支、diff 查看与最近 50 条历史；提交框支持 ⌘↵ / Ctrl+↵ 直接提交，冲突文件不会被误暂存
- **AI 生成提交信息**：Git 面板暂存后可一键生成提交信息——参考暂存 diff、完整文件清单和最近提交的语言与风格，生成结果只填入输入框等人过目，不会自动提交；未配置默认模型会明确提示
- **全局搜索**：侧边栏「搜索」现在可以全文搜索所有会话消息，结果带高亮片段，点击直达对应会话
- **计划以文件形式存在**：计划改为写入工作区 `.plan/` 目录下的 Markdown 文件，审批卡支持复制、导出与打开完整计划；计划文件在编辑器里被改动后，回执卡预览会自动刷新

### 改动

- 侧边栏「每日回顾」入口移除，由 Git 面板取代；旧版计划面板与计划进度展示移除，计划相关交互收敛为审批卡与回执卡
- 交互面板重构，支持键盘操作：数字键 1–9 直接选择动作、方向键切换、Enter 确认、Esc 收起输入区；问答题一次只显示一道，顶部切换条兼作进度表；「要求修改」「修改参数后允许」改为就地展开输入框，且编辑参数时不再显示「以后都允许」，避免把原始参数写成规则
- 旧会话里的 normal / goal 模式统一归为代码模式
- 输入框下方显示整个会话的累计 token 与费用，回合结束立即入账；存在未定价或混合币种的调用时显示「—」而不是部分合计
- 「减弱动态效果」现在对全部动画生效（此前部分动效不受该设置控制）；加载圈在该设置下改为不透明度呼吸而非继续旋转，选「关闭动效」才完全静止
- 主题新增 `stroke` 描边 token：卡片与面板的装饰性描边不再受可读性对比度护栏影响，不再出现一圈中灰实线
- 子代理卡片改用各自的标识色描边与状态配色，运行中不再显示当前工具与调用计数
- 内层标签条：「+」按钮固定显示、滚动边缘渐隐、支持滚轮横滚、活动标签自动滚入视野
- 内容提示改为键盘可达（Tab 聚焦即显示）、不再被滚动容器裁剪、贴近屏幕边缘时自动翻转到视口内
- 工具时间线分组调整：一组工具全部完成后再出现的工具会另起一组

### 修复

- 修复 Windows 上「删除并退出」实际删不掉数据的问题：Chromium 占用句柄的缓存目录现在记账后推迟到下次启动补删，自己的数据不再被整体回滚；同时修复账户专属配置目录被会话清理误删的问题
- 修复计划回执卡片始终贴在整段对话底部、关不掉的问题：现在锚定在当初提出计划的那一轮对话流中，即使审批落盘晚于新一轮提问也不会跑位；同一计划的多次审批只保留最后一条
- 修复英文界面下提示词缓存不支持等错误仍显示中文的问题

### 测试

- 新增配置同步加密、HTTP 通道与每账户同步状态机的测试（4 个文件）
- 新增模式加载、plan-file 工具、启动补删与存储占用回退的测试
- 新增交互面板键盘导航与卡片行为测试（interaction-keys 14 条、interaction-panel 13 条）
- 计划回执锚定测试改写，并补充「回执晚于新一轮提问落盘」「找不到发起消息时退回」两条用例

## v0.1.22

### 新增

- **完整的 OAuth 认证流程重构**：支持 ZCode、BigModel、Z.ai 等多家 issuer，新增本地 loopback 服务器实现授权码回调，优化用户授权体验
- **Ollama 云端支持**：实现非传统 OAuth 的 SSH 签名认证模式（类似 SSH 免密登录），复用官方 Ollama CLI 的密钥文件 `~/.ollama/id_ed25519`，支持用户在官方 CLI 和本应用间无缝切换
- **模型导入系统全面升级**：采用纯函数推导 ID 策略（`hash(转换器版本, sourceId, uuid)`），实现单项提交事务和冲突检测，保证导入数据一致性且永不覆盖用户修改
- **用量中心独立面板**：新增专属页面展示模型、活动和费用数据维度，提供图表组织与用量概览

### 改动

- 优化模型列表获取逻辑，支持 OAuth 认证的上游模型拉取
- 扩展模型绑定和预设管理，支持导入时灵活配置协议覆盖
- 完善导入对话界面，新增导入源选择和预览快照机制
- 增强凭证解析流程，区分订阅、API Key 和平台账户的绑定方式
- 统一思考内容格式适配，支持不同厂商的思考输出标准化
- 补充 Ollama、导入、模型绑定等国际化文案

### 修复

- 修复 ESLint 配置与实际代码的不一致
- 修复模型导入时缺失 `catalogOverrides` 导致工具调用能力异常的问题
- 修复子代理报告展示不完整的问题

### 测试

- 新增 OAuth 流程全链路与错误场景测试（451 行）
- 新增 Ollama SSH 签名、密钥绑定和可用性探针测试（244 行）
- 新增导入扫描、思考适配和子代理报告等测试覆盖（+478 行）

## v0.1.21

### 修复

- 修复发布构建中的 renderer TypeScript 类型错误，统一 i18n 计数消息参数类型
- 修正导入 IPC 合约类型，使 OpenCode 来源能够通过渲染层导入服务正常传递

## v0.1.20

### 修复

- 修复应用重启后后台子代理结果无法继续回传的问题：缺少恢复时的发送配置时，现在会明确标记为阻塞状态，不再静默结束并留下无响应的汇报按钮
- 修复后台子代理汇报请求的内部标记和结果内容传递，确保恢复后补充工作区配置即可继续向主代理汇报

### 测试

- 新增后台子代理跨重启恢复、阻塞和 fallback 汇报路径的测试覆盖

## v0.1.19

### 新增

- 新增供应商配置导入与导出能力，支持加密导出、受控传输和导入校验，便于在不同环境之间迁移模型配置
- 新增 OpenCode 数据导入，支持解析供应商配置与会话记录；同时完善 Claude Code 导入流程及相关兼容处理
- 新增子代理任务队列，支持任务排队、并发调度、取消和状态跟踪，提升复杂任务编排能力
- 用量统计页面独立为设置中的用量中心，新增用量概览与图表组织，并补充模型、活动和费用数据展示
- 聊天内容新增差异对比展示；文件查看器新增 HTML 预览能力，改善代码和生成文件的查看体验

### 改动

- 重构供应商目录、模型面板和导入页面，统一模型选择、协议配置与供应商管理流程
- 完善 Agent、工具注册和运行时生命周期管理，优化子代理调用、会话恢复和任务状态同步
- 优化聊天线程、工具详情、文档状态和扩展资源编辑流程，补充更多中英文 i18n 文案
- 扩展用量汇总、活动记录和模型选择领域模型，并为导入导出、子代理队列和聊天差异补充测试覆盖

### 修复

- 修复供应商配置、模型协议和历史数据在导入或恢复过程中可能丢失字段的问题
- 修复多类聊天、文件预览和设置页面状态更新及布局交互问题
- 修复后台运行、任务调度和用量统计边界场景，提升应用运行稳定性

## v0.1.18

### 新增

- 外层标签页支持右键置顶与取消置顶，置顶标签自动排在前面并持久化保存；标签过多时支持横向滚动，并可通过下拉列表查看和切换当前不可见的标签
- Composer 新增「思考强度」快捷调节、上下文菜单和「最大上下文」开关；支持通过 `/compact` 或双击上下文圆环立即压缩当前上下文
- 上下文管理新增机械压缩检查点、消息流压缩分隔线、压缩详情和节省 token 提示；子代理与定时任务会继承「最大上下文」设置
- Dialog、Menu、ContextMenu、SettingsOverlay 等交互补充统一的入场／退场动效，按钮与滑杆增加按压和拖动反馈，并支持 reduced-motion 偏好

### 改动

- 将上下文窗口拆分为协议窗口、有效窗口和压缩阈值：默认有效窗口限制在 272K 计费分界以内，开启「最大上下文」后可使用模型完整协议窗口，并根据真实定价显示长上下文费用提示
- 将思考强度从模型选择菜单的深层配置移到 Composer 工具栏，支持滑杆、键盘方向键和拖动中的实时档位预览
- 区分自动机械压缩、模型摘要压缩和手动压缩的状态与反馈，保留压缩位置和用量信息，恢复历史时不会把机械压缩统计误当成对话摘要
- 消息流的重生成、删除回合、末轮反馈和压缩分隔线共用稳定的回合定位逻辑，旧会话和缺少锚点的检查点保留顶部面板兜底展示

### 修复

- 修复标签页占满后 `+` 按钮被最后一个标签的关闭按钮覆盖、导致只能点到 `×` 的问题
- 修复标签页横向滚动条撑高标签行并造成标签和操作按钮变形的问题
- 修复快速连续右键时旧 ContextMenu 关闭定时器误关闭新菜单的竞态
- 修复设置面板关闭动画切换页面时的闪烁，并避免动画 transform 影响内部固定定位菜单
- 修复旧工作区配置和数据导入缺少 `maxContext` 字段时的兼容性问题

## v0.1.17

### 新增

- OpenCode Go 供应商新增「协议按模型钉」表:该家三个端点(`/responses`、`/chat/completions`、`/messages`)共用同一个 base,出厂协议只能选一个,而它对表里三分之二的模型是错的,选错的表现是一句读不出与协议有关的 `500 Internal server error`;现按官方 Endpoints 表逐模型钉定协议,文档表之外的模型再按家族前缀兜底,已配置好的老账号在启动时一次性回填
- 等待回复期间,状态行新增一组随机轮换的中 / 英文趣味文案(4 秒一换),缓解长时间空等时界面「像卡住了」的观感;读屏仍朗读固定的「正在等待回复…」,轮换文案对无障碍访问不可见
- 流式 Markdown 改用 Streamdown 按块渲染(已提交的块不再重新解析),配合代码高亮改为复用前缀 span 而非整段重新着色,消除长回复流式输出时的卡顿与代码块闪烁

### 修复

- 修复应用退出时登录态刷新(5 分钟)与配置同步(5 秒)两个后台定时器未被清理的问题:此前它们会在 `closeDatabase` 封库后继续触发,拿到 `DatabaseClosedError` 且无人捕获,在控制台留下未处理的 Promise rejection
- 修复窗口销毁竞态下 `WindowRegistry.send` 可能抛出 "Render frame was disposed" 未被捕获的问题:`isDestroyed()` 判的是 webContents 而非当前渲染帧,重载 / 关闭过程中帧可能先一步销毁

## v0.1.16

### 修复

- 修复 macOS 应用启动即闪退的问题:打包时 Electron fuse 的写入会重写 Framework 内部字节、让此前打的 ad-hoc 签名作废,应用启动读取 fuse 时被内核以 Code Signature Invalid 杀掉;现改为在打包钩子里先翻转 fuse 再签名,并对 codesign 因扩展属性失败的情况重试

## v0.1.15

### 新增

- 客户端登录支持多 Team 账户:登录后按需拉取可选 Team 列表,新增 `ClientTeamSelectionView` 供用户选择 Team,选定前暂缓 `/account`、用量等接口调用

### 修复

- 修复 macOS 签名时 xattr 属性的清理逻辑

## v0.1.14

### 新增

- 扩展页面拆分为 Agents / Commands / Markdown 三个子面板，共享统一的资源列表与 frontmatter 编辑表单
- frontmatter 解析模块新增序列化能力(`serializeFrontmatter`)，供应用内编辑命令/子代理时把表单写回 `.md` 文件
- 补充 SSH 集成测试与相关文档

### 修复

- 修复 Composer 中斜杠命令选择与展开逻辑
- 修复 frontmatter `unquote` 因链式 replace 导致的转义顺序 bug
- 修复 extensions 相关 i18n 函数缺少显式参数类型的问题

### 改动

- 优化聊天响应性相关逻辑，新增配套 QA 脚本用于回归验证
- 清理 MarkdownResourceEditor 冗余的 onFrontmatter prop

## v0.1.13

### 新增

- 新增斜杠命令系统:支持从磁盘加载自定义命令,在 Composer 中输入 `/` 即可与 Skill 混合检索、方向键连续切换;内置命令副标题走 i18n,磁盘命令取 frontmatter 的 description,命令正文在提交时才展开
- 新增远程环境(SSH)支持:可创建与管理 SSH 连接及远程工作区,涵盖 SFTP 文件读写、askpass 交互式认证、原生终端接入,MCP 服务调用也随环境一并转发到远程
- 扩展页面拆分为 Agents / Commands / Markdown 三个子面板,共享统一的资源列表(ResourcePanel/ResourceTable)与 frontmatter 编辑表单
- frontmatter 解析模块新增序列化能力(`serializeFrontmatter`),供应用内编辑命令/子代理时把表单写回 `.md` 文件

### 修复

- 修复 Composer 未接通斜杠命令选择与展开逻辑的问题:此前敲 `/` 只能触发 Skill,命令一条都进不去
- 修复 frontmatter `unquote` 因链式 replace 导致的转义顺序 bug:值中的反斜杠会被后续的换行转义规则误吞,写入再读出后发生损坏
- 修复 extensions 相关 i18n 函数缺少显式参数类型的问题

### 改动

- 优化聊天响应性相关逻辑,新增配套 QA 脚本用于回归验证
- 清理 MarkdownResourceEditor 冗余的 onFrontmatter prop

## v0.1.12

### 新增

- Agent 工具调用授权支持“以后都允许”:在交互面板选择后会将对应规则写入 `.next-cowork/settings.local.json`(仅本机生效),后续匹配的调用自动放行,无需重复确认
- 模型编辑弹窗支持单独设置协议覆盖(跟随供应商 / Anthropic / OpenAI Chat / OpenAI Responses),此前仅新增模型时可选,现有模型也可随时调整

### 修复

- 修复 Agent 在右侧工作台打开新页面(如浏览器)时,对应标签被错误并入主区第一个对话分组的问题;标签页现在按其所属分栏正确归位,并在打开时置顶显示

## v0.1.11

### 修复

- 修复 Windows 平台更新安装包在下载后缺少 `.exe` 后缀导致无法直接打开/安装的问题，确保自动更新流程可以正确执行
- 修复平台登录同步模型时旧别名缺少 `catalogOverrides` 导致工具调用等能力显示异常的问题，恢复模型编辑页与内置目录的一致性

## v0.1.10

### 修复

- 修复 macOS 应用在 Apple Silicon 上启动立即闪退的问题(崩溃提示 Code Signature Invalid):打包时 Electron fuse 写入会发生在签名之后并令签名失效,现在会在写入后重新签名

## v0.1.9

### 修复

- 修复 macOS 安装包在 Apple Silicon 上首次打开被系统提示“已损坏，无法打开”的问题:打包阶段现在会对应用做 ad-hoc 签名,用户只会看到可正常绕过的“未知开发者”提示

## v0.1.8

### 改动

- 内置模型目录按厂商拆分为独立文件(原单文件 3000+ 行合并维护),便于新增和排查具体厂商的模型定义;拆分前后模型数据逐条校验一致,不影响已有模型与功能

## v0.1.7

### 新增

- 增加执行计划(Plan)能力:Agent 可创建并增量更新结构化计划(标题、摘要、步骤、依赖、验收标准、风险与验证项),提交计划后等待用户审批,交互面板与审批卡片同步展示计划内容与状态
- 增加独立的图像模型管理页:集中管理图像生成模型,内置 OpenAI、Grok 订阅、Gemini API、百炼按量 API、GPTNB、RoutinAI 等预设,按模态或图像输出能力自动筛选
- 模型绑定支持单独覆盖协议(Anthropic / OpenAI Chat / OpenAI Responses),不再强制跟随供应商默认协议
- 导入模型列表时按模态(文本/图像)过滤,避免把类型不匹配的模型导入错误的分组

### 改动

- 精简自定义供应商创建流程,移除订阅相关配置项;供应商预设同步移除订阅标记
- 工具调用耗时的计算与展示统一收敛到共享模块,避免主进程日志、运行摘要与界面显示出现不一致的耗时数字
- 发布工作流改为构建产物直接上传到草稿 Release,不再经过 Actions 制品存储,降低配额占用并简化发布排查

## v0.1.6

### 新增

- 增加 Skill 市场与管理页：支持搜索、分类筛选、查看详情、全局或工作区启用、触发统计与本地诊断
- 支持从市场或本地 ZIP 安装 Skill，并可卸载；安装包校验 SHA-256、目录结构、文件数量与解压体积，拒绝路径穿越和符号链接
- 随应用附带 Skill Creator 示例，首次启动时安全安装到全局 Skill 目录，同时保留用户已有版本
- 对话输入框支持通过斜杠命令和附件菜单选择 Skill，以结构化引用插入草稿；技能管理页可直接把选中的 Skill 带回当前对话
- 增加主题工作室与主题库：支持保存、复制、重命名和切换主题，实时预览壁纸、配色、面板材质、字体与动效，并提供对比度保护
- 增加账户基础配置同步：支持首次合并预览、持续同步与冲突处理，同步供应商、模型、偏好和个性化配置；会话、附件及本地路径仍留在设备上
- 模型预设增加 DeepSeek V4.1 Flash 预览模型

### 改动

- 文件默认在右侧工作台打开，重复打开复用已有标签；旧布局中的文件标签会在重新打开时移入右侧面板
- 主题图片支持缩略图与素材信息，增加焦点、裁剪、铺设范围、区域透明度和浅色／深色独立配色；已有主题设置自动迁移到主题库
- Skill 启用状态支持明确选择“全部禁用”，每次运行使用独立的技能快照，并记录使用次数与最近触发时间
- GitHub Release 正文和客户端更新说明使用对应版本的更新日志

### 修复

- 修复主区域最后一个对话标签可能被关闭或移走的问题，并在恢复历史布局时保留主区域对话
- 修复技能管理页切回对话时引用可能丢失的问题，保留原有草稿与输入选区
- 修复 Windows 上 Skill 安装路径判断使用错误分隔符，导致正常安装包被拒绝的问题

## v0.1.5

### 新增

- 增加应用内自动更新：启动后检查新版本，支持下载进度提示、一键安装并重启，顶部同时显示更新提示条
- 关于页可手动检查更新并查看当前更新状态
- 发布流程支持强制更新参数（强制更新标记、最低支持版本、宽限期），并新增 macOS 更新包（.zip）产物
- 会话用量按运行归属记录，重启后历史用量不再丢失

### 改动

- OAuth 流程支持自定义授权码参数名，便于适配回调参数命名不同的供应商

### 修复

- 修复发布上传脚本未给发布通道设置默认值，导致 CI 上传安装包时报 `Invalid value "undefined" for header "X-Client-Channel"` 而中断的问题

## v0.1.4

### 新增

- 增加 NextCoWork 桌面端账户体系：支持网页登录、离线使用、登录态续期，并在账户页查看余额与模型调用用量
- 增加平台托管供应商，登录后自动接入平台模型；同时补充智谱 BigModel 与 Z.ai 的 OAuth 登录支持
- 增加结构化 `AskUserQuestion` 交互：支持单选、多选、多问题切换、补充自由文本，并以选项卡形式在会话中作答
- 增加客户端更新检查，以及发布安装包上传到 NextCoWork 更新服务的脚本与 CI 流程

### 改动

- 扩展供应商凭证与模型解析逻辑，使订阅登录、API 密钥和平台账户能够按各自来源稳定绑定
- 发布流程会在创建 GitHub Release 前校验各平台安装包，并计算 SHA-256 后登记到客户端更新服务
- OAuth 流程拆分共享实现与错误类型，避免循环依赖并提高不同登录渠道的复用性

### 修复

- 修复长串文本在消息气泡中溢出、无法正确换行的问题
- 修复账户供应商同步时可能覆盖用户已调整协议与模型配置的问题

## v0.1.3

### 新增

- 输入框支持 `@` 文件检索：在主进程里对已经遍历好的索引做模糊匹配，只回传十几条候选，选中后以文件引用形式插入会话，可直接点开
- 支持自定义供应商：配置任意 API 兼容的 Base URL 与密钥，创建后拉取模型列表，自建中转、私有部署等预设表里没有的都能接进来
- 输入框草稿改为富文本草稿：文件引用、提及等以结构化条目渲染，光标与 DOM 实时同步

### 改动

- 供应商面板重做：添加模型支持自定义模型 ID、可返回预设目录，按钮与可用状态跟随实际流程
- 附件选择统一走主进程对话框，图片落盘、非图片回传真实路径，与拖拽/粘贴共用同一套分流规则
- 内置主题渐变的来源区分内置图与上传图，分别处理渲染细节

## v0.1.2

### 新增

- 供应商支持账号登录（OAuth 授权码 + PKCE，回环端口接收回调），首家接入 ChatGPT / Codex 订阅；登录态、进度提示、退出登录都在模型设置页里
- 对话回合操作条：复制回复、导出为 Markdown、重新生成（可换模型重来）、删除这一轮
- 插话：正在生成时引入的条目会在下一个轮次边界注入当前 run
- 子代理会话：子代理的运行记录单独落库，可以展开查看
- 消息里的文件引用可直接点开，工作区外的绝对路径也能落得到
- Windows/Linux 自绘窗口控制按钮，最大化状态跟随窗口实际变化

### 改动

- 智能上下文管理改为默认关闭，存量库随迁移一起翻过来
- 代理开关关闭时改为「跟随系统代理」而不是强制直连——此前会让开箱即用状态绕过系统代理，表现为 OAuth 换取凭证时报 403 `unsupported_country_region_territory`
- 供应商面板重做：模型的添加、启用、导入流程都能走通，按钮状态跟随实际可用性
- 模型绑定记住「是哪一家给的」，不再被 priority 更高的另一家顶掉
- 内置文件与搜索工具的路径校验收口到统一的 path-guard
- 上游错误信息带上具体原因；指定的供应商被删除或停用时明确告知不会自动兜底
- 我们自己发出的 API 请求（模型上游、模型列表、搜索服务）带上 `NextCoWork/<版本> (平台; 架构)` 的 User-Agent；内置浏览器、抓网页的工具、OAuth 通道刻意保持原样
- 限流（429）的自动退避从 500ms 起步改成秒级起步，上游给了 `Retry-After` 则一切以它为准（截断在 1 分钟）；并且同一家供应商一旦被限流，进程内其它请求会在发出之前先把这次退避等掉——此前多个并发子代理各自独立退避，同时撞、同时退，把本来够用的配额彼此打光

### 修复

- 修正重试与供应商切换的状态行文案，能看出为什么在重试
- 子代理卡片现在也显示退避重试与供应商切换：此前这两条提示只画在主对话的状态行上，而子代理不看状态行，于是它的重试全程不可见，几次退避都失败后只剩一句限流错误，看起来像子代理根本没有重试
- OpenAI Responses 流式解码补齐若干缺失分支

## v0.1.1

- 新增快捷键设置功能，支持用户自定义打开设置的组合键
- 实现 Windows/Linux 自绘标题栏，支持动态配色和窗口控制
- 更新 Electron 配置以支持正确的用户数据目录和文件协议权限
- 优化 MermaidBlock 组件的图像尺寸处理，并精简其状态管理

## v0.1.0

- 首个发布版本
