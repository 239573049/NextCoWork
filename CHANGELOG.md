# 更新日志

## v0.1.5

### 新增

- 增加应用内自动更新：启动后检查新版本，支持下载进度提示、一键安装并重启，顶部同时显示更新提示条
- 关于页可手动检查更新并查看当前更新状态
- 发布流程支持强制更新参数（强制更新标记、最低支持版本、宽限期），并新增 macOS 更新包（.zip）产物
- 会话用量按运行归属记录，重启后历史用量不再丢失

### 改动

- OAuth 流程支持自定义授权码参数名，便于适配回调参数命名不同的供应商

### 修复

- 修复发布上传脚本未给发布通道设置默认值，导致 CI 上传安装包时报 `Invalid value "undefined" for header "X-Client-Channel"` 而中断的问题

## v0.1.4

### 新增

- 增加 NextCoWork 桌面端账户体系：支持网页登录、离线使用、登录态续期，并在账户页查看余额与模型调用用量
- 增加平台托管供应商，登录后自动接入平台模型；同时补充智谱 BigModel 与 Z.ai 的 OAuth 登录支持
- 增加结构化 `AskUserQuestion` 交互：支持单选、多选、多问题切换、补充自由文本，并以选项卡形式在会话中作答
- 增加客户端更新检查，以及发布安装包上传到 NextCoWork 更新服务的脚本与 CI 流程

### 改动

- 扩展供应商凭证与模型解析逻辑，使订阅登录、API 密钥和平台账户能够按各自来源稳定绑定
- 发布流程会在创建 GitHub Release 前校验各平台安装包，并计算 SHA-256 后登记到客户端更新服务
- OAuth 流程拆分共享实现与错误类型，避免循环依赖并提高不同登录渠道的复用性

### 修复

- 修复长串文本在消息气泡中溢出、无法正确换行的问题
- 修复账户供应商同步时可能覆盖用户已调整协议与模型配置的问题

## v0.1.3

### 新增

- 输入框支持 `@` 文件检索：在主进程里对已经遍历好的索引做模糊匹配，只回传十几条候选，选中后以文件引用形式插入会话，可直接点开
- 支持自定义供应商：配置任意 API 兼容的 Base URL 与密钥，创建后拉取模型列表，自建中转、私有部署等预设表里没有的都能接进来
- 输入框草稿改为富文本草稿：文件引用、提及等以结构化条目渲染，光标与 DOM 实时同步

### 改动

- 供应商面板重做：添加模型支持自定义模型 ID、可返回预设目录，按钮与可用状态跟随实际流程
- 附件选择统一走主进程对话框，图片落盘、非图片回传真实路径，与拖拽/粘贴共用同一套分流规则
- 内置主题渐变的来源区分内置图与上传图，分别处理渲染细节

## v0.1.2

### 新增

- 供应商支持账号登录（OAuth 授权码 + PKCE，回环端口接收回调），首家接入 ChatGPT / Codex 订阅；登录态、进度提示、退出登录都在模型设置页里
- 对话回合操作条：复制回复、导出为 Markdown、重新生成（可换模型重来）、删除这一轮
- 插话：正在生成时引入的条目会在下一个轮次边界注入当前 run
- 子代理会话：子代理的运行记录单独落库，可以展开查看
- 消息里的文件引用可直接点开，工作区外的绝对路径也能落得到
- Windows/Linux 自绘窗口控制按钮，最大化状态跟随窗口实际变化

### 改动

- 智能上下文管理改为默认关闭，存量库随迁移一起翻过来
- 代理开关关闭时改为「跟随系统代理」而不是强制直连——此前会让开箱即用状态绕过系统代理，表现为 OAuth 换取凭证时报 403 `unsupported_country_region_territory`
- 供应商面板重做：模型的添加、启用、导入流程都能走通，按钮状态跟随实际可用性
- 模型绑定记住「是哪一家给的」，不再被 priority 更高的另一家顶掉
- 内置文件与搜索工具的路径校验收口到统一的 path-guard
- 上游错误信息带上具体原因；指定的供应商被删除或停用时明确告知不会自动兜底
- 我们自己发出的 API 请求（模型上游、模型列表、搜索服务）带上 `NextCoWork/<版本> (平台; 架构)` 的 User-Agent；内置浏览器、抓网页的工具、OAuth 通道刻意保持原样
- 限流（429）的自动退避从 500ms 起步改成秒级起步，上游给了 `Retry-After` 则一切以它为准（截断在 1 分钟）；并且同一家供应商一旦被限流，进程内其它请求会在发出之前先把这次退避等掉——此前多个并发子代理各自独立退避，同时撞、同时退，把本来够用的配额彼此打光

### 修复

- 修正重试与供应商切换的状态行文案，能看出为什么在重试
- 子代理卡片现在也显示退避重试与供应商切换：此前这两条提示只画在主对话的状态行上，而子代理不看状态行，于是它的重试全程不可见，几次退避都失败后只剩一句限流错误，看起来像子代理根本没有重试
- OpenAI Responses 流式解码补齐若干缺失分支

## v0.1.1

- 新增快捷键设置功能，支持用户自定义打开设置的组合键
- 实现 Windows/Linux 自绘标题栏，支持动态配色和窗口控制
- 更新 Electron 配置以支持正确的用户数据目录和文件协议权限
- 优化 MermaidBlock 组件的图像尺寸处理，并精简其状态管理

## v0.1.0

- 首个发布版本
