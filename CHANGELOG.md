# 更新日志


## v0.1.22

### 新增

- 完善 OAuth 认证流程与模型导入系统

## v0.1.21

### 修复

- 修复发布构建中的 renderer TypeScript 类型错误，统一 i18n 计数消息参数类型
- 修正导入 IPC 合约类型，使 OpenCode 来源能够通过渲染层导入服务正常传递

## v0.1.20

### 修复

- 修复应用重启后后台子代理结果无法继续回传的问题：缺少恢复时的发送配置时，现在会明确标记为阻塞状态，不再静默结束并留下无响应的汇报按钮
- 修复后台子代理汇报请求的内部标记和结果内容传递，确保恢复后补充工作区配置即可继续向主代理汇报

### 测试

- 新增后台子代理跨重启恢复、阻塞和 fallback 汇报路径的测试覆盖

## v0.1.19

### 新增

- 新增供应商配置导入与导出能力，支持加密导出、受控传输和导入校验，便于在不同环境之间迁移模型配置
- 新增 OpenCode 数据导入，支持解析供应商配置与会话记录；同时完善 Claude Code 导入流程及相关兼容处理
- 新增子代理任务队列，支持任务排队、并发调度、取消和状态跟踪，提升复杂任务编排能力
- 用量统计页面独立为设置中的用量中心，新增用量概览与图表组织，并补充模型、活动和费用数据展示
- 聊天内容新增差异对比展示；文件查看器新增 HTML 预览能力，改善代码和生成文件的查看体验

### 改动

- 重构供应商目录、模型面板和导入页面，统一模型选择、协议配置与供应商管理流程
- 完善 Agent、工具注册和运行时生命周期管理，优化子代理调用、会话恢复和任务状态同步
- 优化聊天线程、工具详情、文档状态和扩展资源编辑流程，补充更多中英文 i18n 文案
- 扩展用量汇总、活动记录和模型选择领域模型，并为导入导出、子代理队列和聊天差异补充测试覆盖

### 修复

- 修复供应商配置、模型协议和历史数据在导入或恢复过程中可能丢失字段的问题
- 修复多类聊天、文件预览和设置页面状态更新及布局交互问题
- 修复后台运行、任务调度和用量统计边界场景，提升应用运行稳定性

## v0.1.18

### 新增

- 外层标签页支持右键置顶与取消置顶，置顶标签自动排在前面并持久化保存；标签过多时支持横向滚动，并可通过下拉列表查看和切换当前不可见的标签
- Composer 新增「思考强度」快捷调节、上下文菜单和「最大上下文」开关；支持通过 `/compact` 或双击上下文圆环立即压缩当前上下文
- 上下文管理新增机械压缩检查点、消息流压缩分隔线、压缩详情和节省 token 提示；子代理与定时任务会继承「最大上下文」设置
- Dialog、Menu、ContextMenu、SettingsOverlay 等交互补充统一的入场／退场动效，按钮与滑杆增加按压和拖动反馈，并支持 reduced-motion 偏好

### 改动

- 将上下文窗口拆分为协议窗口、有效窗口和压缩阈值：默认有效窗口限制在 272K 计费分界以内，开启「最大上下文」后可使用模型完整协议窗口，并根据真实定价显示长上下文费用提示
- 将思考强度从模型选择菜单的深层配置移到 Composer 工具栏，支持滑杆、键盘方向键和拖动中的实时档位预览
- 区分自动机械压缩、模型摘要压缩和手动压缩的状态与反馈，保留压缩位置和用量信息，恢复历史时不会把机械压缩统计误当成对话摘要
- 消息流的重生成、删除回合、末轮反馈和压缩分隔线共用稳定的回合定位逻辑，旧会话和缺少锚点的检查点保留顶部面板兜底展示

### 修复

- 修复标签页占满后 `+` 按钮被最后一个标签的关闭按钮覆盖、导致只能点到 `×` 的问题
- 修复标签页横向滚动条撑高标签行并造成标签和操作按钮变形的问题
- 修复快速连续右键时旧 ContextMenu 关闭定时器误关闭新菜单的竞态
- 修复设置面板关闭动画切换页面时的闪烁，并避免动画 transform 影响内部固定定位菜单
- 修复旧工作区配置和数据导入缺少 `maxContext` 字段时的兼容性问题

## v0.1.17

### 新增

- OpenCode Go 供应商新增「协议按模型钉」表:该家三个端点(`/responses`、`/chat/completions`、`/messages`)共用同一个 base,出厂协议只能选一个,而它对表里三分之二的模型是错的,选错的表现是一句读不出与协议有关的 `500 Internal server error`;现按官方 Endpoints 表逐模型钉定协议,文档表之外的模型再按家族前缀兜底,已配置好的老账号在启动时一次性回填
- 等待回复期间,状态行新增一组随机轮换的中 / 英文趣味文案(4 秒一换),缓解长时间空等时界面「像卡住了」的观感;读屏仍朗读固定的「正在等待回复…」,轮换文案对无障碍访问不可见
- 流式 Markdown 改用 Streamdown 按块渲染(已提交的块不再重新解析),配合代码高亮改为复用前缀 span 而非整段重新着色,消除长回复流式输出时的卡顿与代码块闪烁

### 修复

- 修复应用退出时登录态刷新(5 分钟)与配置同步(5 秒)两个后台定时器未被清理的问题:此前它们会在 `closeDatabase` 封库后继续触发,拿到 `DatabaseClosedError` 且无人捕获,在控制台留下未处理的 Promise rejection
- 修复窗口销毁竞态下 `WindowRegistry.send` 可能抛出 "Render frame was disposed" 未被捕获的问题:`isDestroyed()` 判的是 webContents 而非当前渲染帧,重载 / 关闭过程中帧可能先一步销毁

## v0.1.16

### 修复

- 修复 macOS 应用启动即闪退的问题:打包时 Electron fuse 的写入会重写 Framework 内部字节、让此前打的 ad-hoc 签名作废,应用启动读取 fuse 时被内核以 Code Signature Invalid 杀掉;现改为在打包钩子里先翻转 fuse 再签名,并对 codesign 因扩展属性失败的情况重试

## v0.1.15

### 新增

- 客户端登录支持多 Team 账户:登录后按需拉取可选 Team 列表,新增 `ClientTeamSelectionView` 供用户选择 Team,选定前暂缓 `/account`、用量等接口调用

### 修复

- 修复 macOS 签名时 xattr 属性的清理逻辑

## v0.1.14

### 新增

- 扩展页面拆分为 Agents / Commands / Markdown 三个子面板，共享统一的资源列表与 frontmatter 编辑表单
- frontmatter 解析模块新增序列化能力(`serializeFrontmatter`)，供应用内编辑命令/子代理时把表单写回 `.md` 文件
- 补充 SSH 集成测试与相关文档

### 修复

- 修复 Composer 中斜杠命令选择与展开逻辑
- 修复 frontmatter `unquote` 因链式 replace 导致的转义顺序 bug
- 修复 extensions 相关 i18n 函数缺少显式参数类型的问题

### 改动

- 优化聊天响应性相关逻辑，新增配套 QA 脚本用于回归验证
- 清理 MarkdownResourceEditor 冗余的 onFrontmatter prop

## v0.1.13

### 新增

- 新增斜杠命令系统:支持从磁盘加载自定义命令,在 Composer 中输入 `/` 即可与 Skill 混合检索、方向键连续切换;内置命令副标题走 i18n,磁盘命令取 frontmatter 的 description,命令正文在提交时才展开
- 新增远程环境(SSH)支持:可创建与管理 SSH 连接及远程工作区,涵盖 SFTP 文件读写、askpass 交互式认证、原生终端接入,MCP 服务调用也随环境一并转发到远程
- 扩展页面拆分为 Agents / Commands / Markdown 三个子面板,共享统一的资源列表(ResourcePanel/ResourceTable)与 frontmatter 编辑表单
- frontmatter 解析模块新增序列化能力(`serializeFrontmatter`),供应用内编辑命令/子代理时把表单写回 `.md` 文件

### 修复

- 修复 Composer 未接通斜杠命令选择与展开逻辑的问题:此前敲 `/` 只能触发 Skill,命令一条都进不去
- 修复 frontmatter `unquote` 因链式 replace 导致的转义顺序 bug:值中的反斜杠会被后续的换行转义规则误吞,写入再读出后发生损坏
- 修复 extensions 相关 i18n 函数缺少显式参数类型的问题

### 改动

- 优化聊天响应性相关逻辑,新增配套 QA 脚本用于回归验证
- 清理 MarkdownResourceEditor 冗余的 onFrontmatter prop

## v0.1.12

### 新增

- Agent 工具调用授权支持“以后都允许”:在交互面板选择后会将对应规则写入 `.next-cowork/settings.local.json`(仅本机生效),后续匹配的调用自动放行,无需重复确认
- 模型编辑弹窗支持单独设置协议覆盖(跟随供应商 / Anthropic / OpenAI Chat / OpenAI Responses),此前仅新增模型时可选,现有模型也可随时调整

### 修复

- 修复 Agent 在右侧工作台打开新页面(如浏览器)时,对应标签被错误并入主区第一个对话分组的问题;标签页现在按其所属分栏正确归位,并在打开时置顶显示

## v0.1.11

### 修复

- 修复 Windows 平台更新安装包在下载后缺少 `.exe` 后缀导致无法直接打开/安装的问题，确保自动更新流程可以正确执行
- 修复平台登录同步模型时旧别名缺少 `catalogOverrides` 导致工具调用等能力显示异常的问题，恢复模型编辑页与内置目录的一致性

## v0.1.10

### 修复

- 修复 macOS 应用在 Apple Silicon 上启动立即闪退的问题(崩溃提示 Code Signature Invalid):打包时 Electron fuse 写入会发生在签名之后并令签名失效,现在会在写入后重新签名

## v0.1.9

### 修复

- 修复 macOS 安装包在 Apple Silicon 上首次打开被系统提示“已损坏，无法打开”的问题:打包阶段现在会对应用做 ad-hoc 签名,用户只会看到可正常绕过的“未知开发者”提示

## v0.1.8

### 改动

- 内置模型目录按厂商拆分为独立文件(原单文件 3000+ 行合并维护),便于新增和排查具体厂商的模型定义;拆分前后模型数据逐条校验一致,不影响已有模型与功能

## v0.1.7

### 新增

- 增加执行计划(Plan)能力:Agent 可创建并增量更新结构化计划(标题、摘要、步骤、依赖、验收标准、风险与验证项),提交计划后等待用户审批,交互面板与审批卡片同步展示计划内容与状态
- 增加独立的图像模型管理页:集中管理图像生成模型,内置 OpenAI、Grok 订阅、Gemini API、百炼按量 API、GPTNB、RoutinAI 等预设,按模态或图像输出能力自动筛选
- 模型绑定支持单独覆盖协议(Anthropic / OpenAI Chat / OpenAI Responses),不再强制跟随供应商默认协议
- 导入模型列表时按模态(文本/图像)过滤,避免把类型不匹配的模型导入错误的分组

### 改动

- 精简自定义供应商创建流程,移除订阅相关配置项;供应商预设同步移除订阅标记
- 工具调用耗时的计算与展示统一收敛到共享模块,避免主进程日志、运行摘要与界面显示出现不一致的耗时数字
- 发布工作流改为构建产物直接上传到草稿 Release,不再经过 Actions 制品存储,降低配额占用并简化发布排查

## v0.1.6

### 新增

- 增加 Skill 市场与管理页：支持搜索、分类筛选、查看详情、全局或工作区启用、触发统计与本地诊断
- 支持从市场或本地 ZIP 安装 Skill，并可卸载；安装包校验 SHA-256、目录结构、文件数量与解压体积，拒绝路径穿越和符号链接
- 随应用附带 Skill Creator 示例，首次启动时安全安装到全局 Skill 目录，同时保留用户已有版本
- 对话输入框支持通过斜杠命令和附件菜单选择 Skill，以结构化引用插入草稿；技能管理页可直接把选中的 Skill 带回当前对话
- 增加主题工作室与主题库：支持保存、复制、重命名和切换主题，实时预览壁纸、配色、面板材质、字体与动效，并提供对比度保护
- 增加账户基础配置同步：支持首次合并预览、持续同步与冲突处理，同步供应商、模型、偏好和个性化配置；会话、附件及本地路径仍留在设备上
- 模型预设增加 DeepSeek V4.1 Flash 预览模型

### 改动

- 文件默认在右侧工作台打开，重复打开复用已有标签；旧布局中的文件标签会在重新打开时移入右侧面板
- 主题图片支持缩略图与素材信息，增加焦点、裁剪、铺设范围、区域透明度和浅色／深色独立配色；已有主题设置自动迁移到主题库
- Skill 启用状态支持明确选择“全部禁用”，每次运行使用独立的技能快照，并记录使用次数与最近触发时间
- GitHub Release 正文和客户端更新说明使用对应版本的更新日志

### 修复

- 修复主区域最后一个对话标签可能被关闭或移走的问题，并在恢复历史布局时保留主区域对话
- 修复技能管理页切回对话时引用可能丢失的问题，保留原有草稿与输入选区
- 修复 Windows 上 Skill 安装路径判断使用错误分隔符，导致正常安装包被拒绝的问题

## v0.1.5

### 新增

- 增加应用内自动更新：启动后检查新版本，支持下载进度提示、一键安装并重启，顶部同时显示更新提示条
- 关于页可手动检查更新并查看当前更新状态
- 发布流程支持强制更新参数（强制更新标记、最低支持版本、宽限期），并新增 macOS 更新包（.zip）产物
- 会话用量按运行归属记录，重启后历史用量不再丢失

### 改动

- OAuth 流程支持自定义授权码参数名，便于适配回调参数命名不同的供应商

### 修复

- 修复发布上传脚本未给发布通道设置默认值，导致 CI 上传安装包时报 `Invalid value "undefined" for header "X-Client-Channel"` 而中断的问题

## v0.1.4

### 新增

- 增加 NextCoWork 桌面端账户体系：支持网页登录、离线使用、登录态续期，并在账户页查看余额与模型调用用量
- 增加平台托管供应商，登录后自动接入平台模型；同时补充智谱 BigModel 与 Z.ai 的 OAuth 登录支持
- 增加结构化 `AskUserQuestion` 交互：支持单选、多选、多问题切换、补充自由文本，并以选项卡形式在会话中作答
- 增加客户端更新检查，以及发布安装包上传到 NextCoWork 更新服务的脚本与 CI 流程

### 改动

- 扩展供应商凭证与模型解析逻辑，使订阅登录、API 密钥和平台账户能够按各自来源稳定绑定
- 发布流程会在创建 GitHub Release 前校验各平台安装包，并计算 SHA-256 后登记到客户端更新服务
- OAuth 流程拆分共享实现与错误类型，避免循环依赖并提高不同登录渠道的复用性

### 修复

- 修复长串文本在消息气泡中溢出、无法正确换行的问题
- 修复账户供应商同步时可能覆盖用户已调整协议与模型配置的问题

## v0.1.3

### 新增

- 输入框支持 `@` 文件检索：在主进程里对已经遍历好的索引做模糊匹配，只回传十几条候选，选中后以文件引用形式插入会话，可直接点开
- 支持自定义供应商：配置任意 API 兼容的 Base URL 与密钥，创建后拉取模型列表，自建中转、私有部署等预设表里没有的都能接进来
- 输入框草稿改为富文本草稿：文件引用、提及等以结构化条目渲染，光标与 DOM 实时同步

### 改动

- 供应商面板重做：添加模型支持自定义模型 ID、可返回预设目录，按钮与可用状态跟随实际流程
- 附件选择统一走主进程对话框，图片落盘、非图片回传真实路径，与拖拽/粘贴共用同一套分流规则
- 内置主题渐变的来源区分内置图与上传图，分别处理渲染细节

## v0.1.2

### 新增

- 供应商支持账号登录（OAuth 授权码 + PKCE，回环端口接收回调），首家接入 ChatGPT / Codex 订阅；登录态、进度提示、退出登录都在模型设置页里
- 对话回合操作条：复制回复、导出为 Markdown、重新生成（可换模型重来）、删除这一轮
- 插话：正在生成时引入的条目会在下一个轮次边界注入当前 run
- 子代理会话：子代理的运行记录单独落库，可以展开查看
- 消息里的文件引用可直接点开，工作区外的绝对路径也能落得到
- Windows/Linux 自绘窗口控制按钮，最大化状态跟随窗口实际变化

### 改动

- 智能上下文管理改为默认关闭，存量库随迁移一起翻过来
- 代理开关关闭时改为「跟随系统代理」而不是强制直连——此前会让开箱即用状态绕过系统代理，表现为 OAuth 换取凭证时报 403 `unsupported_country_region_territory`
- 供应商面板重做：模型的添加、启用、导入流程都能走通，按钮状态跟随实际可用性
- 模型绑定记住「是哪一家给的」，不再被 priority 更高的另一家顶掉
- 内置文件与搜索工具的路径校验收口到统一的 path-guard
- 上游错误信息带上具体原因；指定的供应商被删除或停用时明确告知不会自动兜底
- 我们自己发出的 API 请求（模型上游、模型列表、搜索服务）带上 `NextCoWork/<版本> (平台; 架构)` 的 User-Agent；内置浏览器、抓网页的工具、OAuth 通道刻意保持原样
- 限流（429）的自动退避从 500ms 起步改成秒级起步，上游给了 `Retry-After` 则一切以它为准（截断在 1 分钟）；并且同一家供应商一旦被限流，进程内其它请求会在发出之前先把这次退避等掉——此前多个并发子代理各自独立退避，同时撞、同时退，把本来够用的配额彼此打光

### 修复

- 修正重试与供应商切换的状态行文案，能看出为什么在重试
- 子代理卡片现在也显示退避重试与供应商切换：此前这两条提示只画在主对话的状态行上，而子代理不看状态行，于是它的重试全程不可见，几次退避都失败后只剩一句限流错误，看起来像子代理根本没有重试
- OpenAI Responses 流式解码补齐若干缺失分支

## v0.1.1

- 新增快捷键设置功能，支持用户自定义打开设置的组合键
- 实现 Windows/Linux 自绘标题栏，支持动态配色和窗口控制
- 更新 Electron 配置以支持正确的用户数据目录和文件协议权限
- 优化 MermaidBlock 组件的图像尺寸处理，并精简其状态管理

## v0.1.0

- 首个发布版本
