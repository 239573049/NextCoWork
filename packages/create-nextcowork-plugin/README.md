# create-nextcowork-plugin

```bash
npm create nextcowork-plugin@latest
```

问三样(publisher / name / 显示名),生成一份**已经能上架**的骨架:
清单字段齐全、两种语言的 l10n 都在、贡献点的 title 全是 `%key%`。
`npm run package` 出来的 ZIP 直接过服务端校验。

它不问「你需要哪些能力」——一个还没跑通 hello world 的人回答不了这个问题。
能力等你真的要用时再往 `permissions` 里加,那时候你知道自己在要什么。
